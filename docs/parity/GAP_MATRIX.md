<!--
  ~ Licensed to the Apache Software Foundation (ASF) under one
  ~ or more contributor license agreements.  See the NOTICE file
  ~ distributed with this work for additional information
  ~ regarding copyright ownership.  The ASF licenses this file
  ~ to you under the Apache License, Version 2.0 (the
  ~ "License"); you may not use this file except in compliance
  ~ with the License.  You may obtain a copy of the License at
  ~
  ~   http://www.apache.org/licenses/LICENSE-2.0
  ~
  ~ Unless required by applicable law or agreed to in writing,
  ~ software distributed under the License is distributed on an
  ~ "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
  ~ KIND, either express or implied.  See the License for the
  ~ specific language governing permissions and limitations
  ~ under the License.
-->

# Java ↔ Rust Capability Gap Matrix

> **Goal:** 1-to-1 capability parity between this Rust implementation and the Apache Iceberg
> **Java `iceberg-core` / `iceberg-api`** library (the engine-agnostic table-format library — *not*
> the Spark engine integration). This is a **living document**: re-run the audit after every upstream
> sync and after each parity phase lands.

## Status legend

- ✅ **present** — implemented to a usable degree
- 🟡 **partial** — exists but incomplete vs Java
- ❌ **missing** — not implemented

## Audit provenance

- **Rust base audited:** owned fork on upstream **`iceberg` 0.9.1** (datafusion 52.2, arrow 57.1,
  parquet 57.1, MSRV 1.92), **re-audited 2026-06-07** after the Phase 0 sync. Source of truth:
  `crates/iceberg/src/{spec,expr,scan,transaction,writer,arrow,io,inspect,puffin,catalog}` plus the
  new `crates/storage/opendal` FileIO crate.
- **Java reference:** `apache/iceberg` `main`, modules `api/` + `core/` + `data/` + `orc/` +
  `parquet/` + `arrow/`.
- **Re-audit policy:** re-run after every upstream sync and after each parity phase; date-stamp this
  block and strike the rows the sync/phase solved.
- **Interop oracle (test-only):** `dev/java-interop/` drives Java `iceberg-core` **1.10.0** as a
  read/write oracle for bidirectional fixtures (not shipped, not a Cargo dependency). One
  `generate`/`verify` pass now covers all THREE Phase-1 evolution capabilities — it backs the
  ✅-by-interop rows `UpdateSchema`, `UpdatePartitionSpec`, and the `ManageSnapshots` ref-operation surface
  (incl. "Snapshot model + refs"), all dated 2026-06-07.
- **What the 0.7→0.9.1 sync changed (flipped rows below):** `timestamp_ns` type, column default
  values (`initial_default`/`write_default`), merge-on-read **read** application of position-deletes +
  deletion-vectors during scan *(2026-06-10 audit correction: the sync brought PARQUET
  position/equality deletes only — the deletion-vector scan path was a loader TODO until
  Increment D1; see the merge-on-read read row)*, the `upgrade_format_version` transaction action, and a real
  `TransactionAction`/`ApplyTransactionAction` extension seam. The **headline gaps are unchanged**:
  write-engine actions, schema/partition/snapshot evolution, incremental scans, ORC/Avro data files,
  variant/geo/unknown types, catalog view ops, maintenance actions, encryption.
- **Cell style (de-triplication pass, 2026-06-10):** this matrix is the ONLY status record (the
  one-home-per-fact rule in [CLAUDE.md](../../CLAUDE.md)). Cells stay TERSE — Rust location, a 1–2
  sentence summary, flip dates, links. The full per-increment narratives the cells used to carry
  moved verbatim to [archive/2026-06_matrix-cell-narratives.md](archive/2026-06_matrix-cell-narratives.md)
  (grep by row Area). When a status flips: update the cell here, link evidence, write the narrative
  in the todo/lessons flow — do NOT grow the cell back.

## Matrix

| Area | Status | Java reference | Rust location / note |
|---|---|---|---|
| Primitive + nested types | ✅ | `api/.../types/Types.java` | `spec/datatypes.rs` |
| V3 types: variant | 🟡 | `api/.../variants/`, `core/.../variants/`, `api/.../types/Types.java` (`VariantType`) | `variant/` — binary format BOTH sides. READ (2026-06-11): metadata dictionary, all 21 primitive type ids, short strings, objects, arrays; eager + bounds-checked (security boundary, divergences in mod.rs doc). WRITE (B2, 2026-06-11): `Variants.metadata`/`Variants.of` factories, `PrimitiveWrapper`/`ValueArray`/`ShreddedObject`-core serialization (`write.rs` — insertion-order dict + compareTo sorted flag, `sizeOf` widths, UTF-16 field sort, 63-byte short-string spill, decimal precision→width) — BYTE-EXACT vs Java-1.10.0-generated fixtures (full-hex + CRC pins incl. 255/256 + 65535/65536 width boundaries) + round-trip vs the B1 reader. **SCHEMA-TYPE ENTRY (F1, 2026-06-11):** `Type::Variant` in `spec/datatypes.rs` (own category like Java's `VariantType implements Type` — not primitive, not nested), JSON serde as the bare string `"variant"` (`SchemaParser` parity), the `MIN_FORMAT_VERSIONS` V3 gate (same `min_format_version`/`check_compatibility` mechanism as `timestamp_ns`, shared-gate-pinned; enforced at `TableMetadataBuilder::add_schema` for creation + evolution), legality doors (not a partition source for any value-producing transform / not a sort key / not an identifier field / no promotion either direction; void+unknown transforms still accept), Avro conversion = Java's record shape (`metadata`+`value` bytes, logicalType `variant`, `r<fieldId>` naming incl. map values, both directions + order-insensitive detection + malformed-record rejection — REVIEWER 2026-06-11: live-Java-probed vs 1.10.0 `AvroSchemaUtil`/`SchemaUpdate`/`TableMetadataParser`, both directions byte-compared; read-tolerance of V1/V2-with-variant metadata and the identity-transform parse door pinned; malformed-shape rejection is a deliberate fail-loud divergence from Java's silent logical-type drop on parse), Arrow conversion = LOUD error naming the arrow-rs 57.1 pin (Java parity: `ArrowSchemaUtil` inherits the visitor-default throw). **SHREDDING OVERLAY write side + VariantVisitor (F2, 2026-06-11):** `variant/shredded.rs` — `ShreddedObject`'s partial-shred surface (`Variants.object(metadata, object)` re-wrap, `put`/`remove`/`removedFields`, `get`/`fieldNames`/`numFields` views) with the `SerializedObject.sliceValue` VERBATIM contract: untouched serialized-backing fields copy their ORIGINAL bytes (non-canonical encodings preserved — the divergence-class pin), shredded values win collisions, removed names excluded, ids/offsets/header recomputed over the MERGED set (`fieldIdSize = sizeOf(dictionarySize)`, `offsetSize = sizeOf(merged dataSize)`), Java's remove-then-put view/serialization disagreement mirrored — 17 Java-1.10.0 byte fixtures (incl. 255/256 + 65535/65536 merged-dataSize straddles, dup-name backings, empty-overlay re-header vs byte-identity) + Java's exact precondition messages; two probe-verified 1.10.0 BUGS deliberately not mirrored (stale `remove()` cache; corrupt first serialization over a constructed backing — MAIN semantics ported, Java's self-healed output pinned as oracle). `variant/visitor.rs` — `VariantVisitor` trait + `visit_variant`/`visit_value` drivers, traversal order pinned against a Java-generated event log (hooks incl. `finally` semantics on the error path; depth-guarded; name-lookup-miss errs where Java NPEs). REVIEWER 2026-06-11: both 1.10.0 bugs independently re-verified (bytecode + live ShredProbe re-run + MAIN diff); +5 adversarial Java-probed pins (ReviewerShredProbe r1-r5) incl. the data-order≠field-order backing that ALONE kills the adjacent-offset-subtraction slice mutation the original fixture set missed, a nested non-canonical subtree, a 4-byte-offset backing, dup-name-removed, and the probe that Java serializes a malformed untouched backing field BLIND (Rust rejects at the door — accepted-set divergence, documented in shredded.rs). Deferred: shredded-parquet FILE I/O + file-level interop — blocked at the pinned parquet 57.1 crate (no variant support; Java 1.10.0 `TypeToMessageType.variant` defines the parquet group, unreachable here without a dependency change); `unknown` type entry (a new PrimitiveType, crate-wide ripple — not the one-line gate arm). |
| V3 types: geometry / geography | ❌ | `api/.../geospatial/` | none |
| V3 types: timestamp_ns | ✅ | `types/Types.java` | `spec/datatypes.rs` (`PrimitiveType::TimestampNs`/`TimestamptzNs`); **V3-only format-version gate enforced** — `Schema::check_compatibility` rejects a `timestamp_ns`/`timestamptz_ns` field (incl. nested) on a V1/V2 table (Java `Schema.MIN_FORMAT_VERSIONS`, "Invalid type for {col}: timestamp_ns is not supported until v3") |
| V3 types: unknown | ❌ | `types/Types.java` | none |
| Column default values (initial/write) | ✅ | `Schema`/`Types` | `spec/datatypes.rs` `NestedField` carries `initial_default`/`write_default` |
| Partition transforms (identity/bucket/truncate/year/month/day/hour/void) | ✅ | `api/.../transforms/` | `spec/transform.rs` |
| Schema evolution (`UpdateSchema`) | ✅ | `api/UpdateSchema.java`, `core/SchemaUpdate.java`, `schema/UnionByNameVisitor.java` | `transaction/update_schema.rs` — full Java `SchemaUpdate` parity: add/rename/update-type/doc/make-optional/require/delete/move/set-identifier-fields, `union_by_name` (full `UnionByNameVisitor`), LEVEL-ORDER fresh field-id assignment, column initial/write defaults, and the V3-only initial-default + type gates (`Schema::check_compatibility` wired into `TableMetadataBuilder::add_schema`). **Interop ✅ 2026-06-07** — bidirectional via `crates/iceberg/tests/interop_update_schema.rs` (7 scenarios). Full narrative + edge cases: [archive](archive/2026-06_matrix-cell-narratives.md). |
| Partition evolution (`UpdatePartitionSpec`) | ✅ | `api/UpdatePartitionSpec.java`, `core/BaseUpdatePartitionSpec.java` | `transaction/update_partition_spec.rs` — full `BaseUpdatePartitionSpec` parity: auto-naming, field-id+name recycling, V1 void replacement, guard rails; the partition-name↔schema collision guard was relaxed to identity-OR-void (a real divergence interop surfaced and fixed). **Interop ✅ 2026-06-07** — bidirectional via `crates/iceberg/tests/interop_update_partition_spec.rs` (7 scenarios). Narrative: [archive](archive/2026-06_matrix-cell-narratives.md). |
| Sort order (`ReplaceSortOrder`) | ✅ | `api/ReplaceSortOrder.java` | `transaction/sort_order.rs` |
| Snapshot model + refs (branches/tags) | ✅ | `api/Snapshot.java`, `SnapshotRef.java`, `core/SnapshotParser.java` | Spec types + ref operations (`transaction/manage_snapshots.rs`). **Interop ✅ 2026-06-07** — the `SnapshotReference`/`SnapshotRetention` model round-trips with Java via `crates/iceberg/tests/interop_manage_snapshots.rs`. Narrative: [archive](archive/2026-06_matrix-cell-narratives.md). |
| Snapshot management (`ManageSnapshots`: branch/tag CRUD, rollback, rollback-to-time, set-current, fast-forward) | ✅ (ref-op surface); 🟡 (`cherrypick` — unit-proven, interop deferred) | `api/ManageSnapshots.java`, `core/SetSnapshotOperation.java`, `core/CherryPickOperation.java`, `api/SnapshotRef.java` | `transaction/manage_snapshots.rs` — branch/tag CRUD + rename, set-current, rollback (ancestry-checked), rollback-to-time (strict `<`), fast-forward, retention (non-positive rejected), no-op suppression. **Interop ✅ 2026-06-07** — 7 scenarios both directions via `crates/iceberg/tests/interop_manage_snapshots.rs`. **`cherrypick` landed 2026-06-11** as a standalone `transaction/cherry_pick.rs` (Java `CherryPickOperation` / WAP publish): append/replace-partitions replay + fast-forward precedence, `source-snapshot-id`/`published-wap-id` tags, validateNonAncestor + double-publish dedup + duplicate-WAP + changed-partition rejections — 16 MemoryCatalog tests, mutations caught; **reviewer-verified 2026-06-11** (precedence matrix confirmed cell-by-cell incl. the concurrent-head-moved cells; reviewer added the dangling-publish dedup-scope negative control, the non-eligible-op fast-forward cell). **Multi-spec replay is now Java PARITY (2026-06-11):** the producer gained per-spec manifest grouping, so cherry-picking an old-spec staged snapshot SUCCEEDS (the replayed file lands in a manifest stamped with its own spec id, scan correct) — the former fail-loud divergence pin was converted to `test_cherrypick_multispec_replay_produces_per_spec_manifest`. **Metadata-level interop ✅ 2026-06-11 (ff / replay / dedup):** proven both directions via `dev/java-interop/run-interop-cherrypick.sh` + `crates/iceberg/tests/interop_cherrypick.rs` — D1 (Rust acts, Java judges): Java's canonical view of each Rust-produced table byte-equals Java's own view; Java's `verify-interop-cherrypick` confirmed per-fixture facts (FF: 2 snapshots; replay/dedup: 4 snapshots + `source-snapshot-id` present; dedup: second cherrypick raises `CherrypickAncestorCommitException`). D2 (Java acts, Rust verifies): Rust's `snapshot_meta_view` of each Java-produced table equals Java's view. **`stageOnly` WAP-write path landed 2026-06-11** (Group V) — `SnapshotProducer::with_stage_only` + `FastAppendAction::stage_only()`/`DeleteFilesAction::stage_only()` (Java `SnapshotProducer.stageOnly()` on the `SnapshotUpdate` interface): a staged commit emits `AddSnapshot` ALONE (no `SetSnapshotRef`), so current-snapshot-id/main/snapshot-log are unchanged on disk and the staged snapshot consumes a sequence number like a normal commit (1.10.0-bytecode-confirmed: `lambda$commit$2` stageOnly branch, `apply()` seq is flag-independent, `addSnapshot` touches no log/ref). `wap.id` rides the existing `set_snapshot_properties`; NO spec/ change (Rust builder already Java-faithful for add-snapshot-without-ref). 14 tests (crown jewel re-parse, scan-unaffected, e2e stage→cherry_pick, two-staged coexist + reverse-publish-order, delete-bearing stage, retention-expirable, mutation-bait) + **reviewer-verified 2026-06-11** (bytecode-cited `UpdateRequirements.forUpdateTable`: the `Builder.update` dispatcher has NO `AddSnapshot` case ⇒ an AddSnapshot-only update derives `AssertTableUUID` only): reviewer added the wire-level exact update/requirement-set pin (`AddSnapshot` alone + `UuidMatch` alone — catches an over-strict staged `RefSnapshotIdMatch` the retry/rebase machinery masks end-to-end), concurrent-publish-vs-stage in BOTH orders, the retry/rebase seq+parent recompute pin, the wap.id-coexists-with-producer-keys pin, and the readable-by-explicit-id/hidden-from-default-scan + inspect-posture (`snapshots` shows it, `history` does not) pin. **WAP-path publish dedup landed 2026-06-11** (Group V V2) — BOTH dedup paths now present in `cherry_pick.rs`: the ancestry path (already-ancestor + `source-snapshot-id` double-publish) AND the `wap.id`-keyed path (`validate_wap_publish`/`is_wap_id_published`, Java `WapUtil.validateWapPublish`/`isWapIdPublished`). The WAP dedup walks the CURRENT ancestry comparing the picked snapshot's non-empty `wap.id` against each ancestor's own `wap.id` AND its `published-wap-id` (BOTH arms — a REPLAY publish stamps `published-wap-id`, a FAST-FORWARD publish keeps only the staged `wap.id`), rejecting a duplicate with the verbatim 1.10.0 `DuplicateWAPCommitException` message; ordering pinned (Java runs validateNonAncestor BEFORE validateWapPublish, so the ancestry error wins when both apply). The core machinery was already present from the V1 cherrypick land; V2 verified it byte-exact against 1.10.0 bytecode and added 5 tests (FF-path dedup via the staged-`wap.id` arm, state-unchanged-on-rejection re-parse, distinct-ids no-false-positive, non-WAP negative control, both-paths ordering pin); 2 mutations caught (disable wap-walk → both crown jewels fail; drop the staged-`wap.id` arm → only the FF-path test fails). **V3 `write.wap.enabled` settled OUT** — ENGINE-side only (1.10.0 bytecode: defined in `TableProperties` but read by no core production class; `SnapshotProducer.apply` never calls `WapUtil`; only Spark's `SparkWriteConf`/`SparkReadConf`/`SparkTableUtil` consume it). **Reviewer-verified 2026-06-11** (production confirmed byte-identical; all four bytecode verdicts + V3-OUT re-derived independently from the m2 jars — the `write.wap.enabled` literal sits in EXACTLY 1 of 1212 core classes, the definition; both builder mutations re-run + the mirror published-arm-drop mutation added, each arm independently pinned): reviewer added `test_cherrypick_rollback_reopens_wap_id_java_faithful` pinning the current-ancestry dedup SCOPE — a WAP publish rolled back past (or orphaned off `main`) reopens its `wap.id` and a second same-id publish succeeds, the identical Java hole (same `currentSnapshot()` walk root), NOT a divergence (fail-before/pass-after against a scope-widening mutation). **Staged-WAP interop ✅ 2026-06-12 (Z1):** THREE FIXTURES (S-ff / S-replay / S-dedup) × TWO VIEWS (staged-but-unpublished + post-publish), BOTH DIRECTIONS, BOTH sides using REAL staging APIs (`stageOnly()` / `stage_only()`). S-dedup redesigned to the `testDuplicateCherrypick` same-parent pattern: stage BOTH w3 snapshots off S0 before any cherry-pick → first cherry-pick FF (parent=S0=head) → second cherry-pick REPLAY (parent=S0≠w3-first=head) → `validate_wap_publish` fires → `DuplicateWAPCommitException` / `DataInvalid`. D1 (Rust acts, Java judges): `SnapshotMetaOracle` byte-diffs BOTH staged + final views; `verify-interop-staged-wap` confirms all per-fixture facts; 4-sabotage battery all closed (7a/7b/7c/7d — incl. the staged-snapshot-removal pin proving the staged-state view IS testing `metadata.snapshots()` coverage). D2 (Java acts, Rust verifies): Rust `snapshot_meta_view` == Java view for staged + final tables. Full chain ×2 green; verbatim gate ×2 (2168 lib tests). Consumers: `dev/java-interop/run-interop-staged-wap.sh`, `crates/iceberg/tests/interop_staged_wap.rs`. Row stays 🟡 (data-level WAP interop open). Narrative: [archive](archive/2026-06_matrix-cell-narratives.md). |
| Manifest + manifest-list read/write | ✅ | `core/.../ManifestReader/Writer` | `spec/manifest`, `spec/manifest_list.rs`. Spec-mandated default-to-0 reads for V1→V2 fields VERIFIED + mutation-pinned 2026-06-07 (snapshot `sequence-number` lenient read fixed; the Avro siblings were already correct). Table-metadata `last-sequence-number` strict-on-V2/V3 read CLOSED Arc G 2026-06-11: Rust ALREADY matches Java 1.10.0 (bytecode — `TableMetadataParser.fromJson` does `JsonUtil.getLong` for `formatVersion>1`, which THROWS on absent; V1 hard-defaults to 0). `#[serde(default)]` was the WRONG fix (would accept V2 metadata Java rejects). Pinned in `spec/table_metadata.rs`. **W3 comparator groundwork (2026-06-11; sort position RESOLVED 2026-06-12, Opus reviewer):** `partition_spec_id` added to the manifest-level emitted JSON on all three sides (`crates/iceberg/tests/common/snapshot_meta_view.rs` + `interop_expire.rs` + Java `SnapshotMetaOracle`); constant 0 in all existing single-spec fixtures — byte-unaffected. The canonical-view sort tuple now carries `partition_spec_id` as the FINAL tiebreaker (position 10, Option B) symmetrically on all three sides — deterministic for future multi-spec fixtures, byte-invisible for single-spec (all five metadata chains stay green). **Z2 multi-spec interop ✅ 2026-06-12:** proven both directions via `dev/java-interop/run-interop-multi-spec.sh` + `crates/iceberg/tests/interop_multi_spec.rs` — single multi-spec fast-append commit produces TWO manifests with `partition_spec_id` 0 and 1 cross-language; the spec-id tiebreaker (position 10) is the SOLE disambiguator for tie-shaped ms4 manifests (F0.record_count == F3.record_count == 10); 4-sabotage battery proves both the spec-id field and the per-own-spec partition rendering are load-bearing (SB4 swaps the two spec field-definitions in the SOURCE metadata and RE-EMITS — the ms4 partition tuples then render under the wrong-arity spec and the view diverges). Narrative: [archive](archive/2026-06_matrix-cell-narratives.md). |
| `RewriteManifests` | 🟡 | `api/RewriteManifests.java`, `core/BaseRewriteManifests.java` | `transaction/rewrite_manifests.rs` — re-organize manifests in one `Operation::Replace` snapshot with the live set unchanged (Java `BaseRewriteManifests`, extends `SnapshotProducer`): `cluster_by` re-groups live data-manifest entries via the provenance-preserving `add_existing_entry` path (original snapshot id + seq numbers kept, status → Existing); `rewrite_if`, `add_manifest`/`delete_manifest` (explicit replacement), `set`; `validateDeletedManifests` + `validateFilesCounts`; delete manifests carried forward byte-identical. 17 `MemoryCatalog` tests incl. the provenance re-stamp mutation pin + a merge-on-read delete-still-applies test; reviewer-added pins (2026-06-10): two-level-inheritance on-disk-seq pin (rewritten manifest stores ORIGINAL seqs explicitly, never null ⇒ resurrection-proof; fails under seq-strip mutation), multi-spec clustering axis (constant key over two partition-spec ids ⇒ one manifest per spec, no cross-merge; fails under spec-id-dropped key), and computed-count-vs-user-`set()` precedence (computed counts win, Java last-write-wins; fails under reversed ordering). Reviewer confirmed `changed-partition-count=0` IS emitted on both sides (parity — Java `SnapshotSummary.Builder.build` L200 with default `trustPartitionMetrics`). Adaptations (documented): String cluster key (vs Java `Object`), estimated-length size rolling (`ManifestWriter` has no incremental length), V1 `add_manifest` → `FeatureUnsupported` (Java `copyManifest` deferred), no-current-snapshot → `DataInvalid` (Java NPEs), `scanManifestsWith` not ported. **Metadata-level interop ✅ 2026-06-10 (cluster-by path):** the canonical snapshot-metadata view of the Rust `cluster_by` rewrite matches Java's `clusterBy` byte-for-byte in the eight-step write-actions chain (s6 re-groups the live data entries one manifest per partition, every entry carried Existing with original provenance), judged by Java itself (`dev/java-interop/run-interop-write-actions.sh` + `crates/iceberg/tests/interop_write_actions_meta.rs`); the add/delete-manifest explicit-replacement mode is NOT covered by the chain. **Landed 2026-06-10; reviewer-verified 2026-06-10; row stays 🟡 (data-level interop open).** |
| Write: fast append | ✅ | `api/AppendFiles.java`, `core/FastAppend.java` | `transaction/append.rs` — Java `newFastAppend`. **All-tombstone-manifest carry FIXED 2026-06-11 (Wave-4 Group O / O1, the A3 STOP-finding):** `FastAppendOperation::existing_manifest` now carries EVERY prior manifest-list entry forward UNFILTERED, matching Java `FastAppend.apply` (`manifests.addAll(snapshot.allManifests(io))`, bytecode offsets 89-94 — NO predicate; `BaseSnapshot.allManifests` returns the list verbatim). The previous `has_added_files()`-OR-`has_existing_files()` filter DROPPED a manifest left ALL-DELETED by a prior copy-on-write delete that emptied it — a manifest-LIST structure divergence on any append-after-emptying-delete history (the dropped manifest became unreferenced/GC-eligible where Java keeps it referenced; bites byte-level interop + tombstone-visibility consumers like orphan-protection / changelog scans). Pinned on-disk: re-parsed-manifest-list fail-before/pass-after reproduction (add → emptying delete → fast_append CONTAINS the all-tombstone manifest), scan-unchanged pin (carry does NOT resurrect deleted rows), merge_append documented-parity pin. Note: the MERGING append (`merge_append`) intentionally keeps the filter — see that row. Interop re-proof deferred (Group S). **Multi-spec fast-append interop ✅ 2026-06-12 (Z2):** a SINGLE `fast_append` call carrying files under two different partition specs produces TWO manifests in ONE snapshot (per-spec manifest grouping), with each manifest stamped with the file's OWN spec id (file's-own-spec rule) — proven cross-language both directions via `run-interop-multi-spec.sh` + `tests/interop_multi_spec.rs`. |
| Write: merge append | 🟡 | `api/AppendFiles.java`, `core/MergeAppend.java`, `core/ManifestMergeManager.java`, `core/util/BinPacking.java` | `transaction/merge_append.rs` — Java `newAppend`: append in one `Operation::Append` snapshot like `fast_append`, then bin-pack + merge the manifest list per `commit.manifest-merge.enabled` / `commit.manifest.min-count-to-merge` / `commit.manifest.target-size-bytes`. Group by spec id (reverse-sorted, never cross-spec), `BinPacking.packEnd` (ported in a private `bin_packing` submodule), three bin rules, provenance-preserving entry routing (carried → Existing + original snapshot id/seqs ON DISK; this-commit → Added + null seq ⇒ re-inherits; old tombstones suppressed). Summary shape == `fast_append` (no `manifests-*` keys — documented divergence from Java's `buildManifestCountSummary`). 19 `MemoryCatalog` tests incl. raw-avro on-disk-seq provenance pin + old-tombstone suppression + MoR-delete-carry crown jewel + `bin_disposition`/`pack_end` units; provenance-restamp + tombstone-suppression + bin-gate mutations verified. **Metadata-level interop ✅ 2026-06-10 (one-bin merge path):** the canonical snapshot-metadata view of the Rust `merge_append` matches Java's `newAppend` byte-for-byte in the eight-step write-actions chain (s7 sets `min-count-to-merge=2` as a table property — no snapshot — then s8 merges every tiny manifest into ONE merged manifest with carried Existing entries + the new Added entry), judged by Java itself (`dev/java-interop/run-interop-write-actions.sh` + `crates/iceberg/tests/interop_write_actions_meta.rs`); multi-bin / multi-spec merge paths NOT covered by the chain. **All-tombstone-manifest carry — SETTLED PARITY (no fix needed), Wave-4 Group O / O1 2026-06-11:** UNLIKE `fast_append` (which carries ALL prior manifests, see that row), the MERGING append DROPS all-tombstone prior manifests, matching Java `MergingSnapshotProducer.apply` (L1007-1011) which filters its carried set through the `shouldKeep` predicate `hasAddedFiles()` OR `hasExistingFiles()` OR `snapshotId() == snapshotId()`. Rust merge_append's `existing_manifest` filter (`has_added_files` OR `has_existing_files`) equals Java's `shouldKeep` minus the third clause, which is UNREACHABLE for a pure append (no carried manifest was written by the not-yet-committed snapshot) ⇒ no divergence. Pinned by `test_merge_append_drops_all_tombstone_manifest_unlike_fast_append` (in `append.rs`). **Deferred:** delete-manifest merging (carried unchanged), `appendManifest`, retry cache/orphan cleanup. **Landed 2026-06-10; row stays 🟡 (data-level interop open).** **Data-level interop ✅ 2026-06-11 (fixture A — S1):** bidirectional via `dev/java-interop/run-interop-write-data.sh` + `crates/iceberg/tests/interop_write_data.rs`. V2 partitioned table (identity(category), schema `{id,category,data}`): fast_append A(cat=a,10/20/30)+B(cat=b,40) → set min-count=2 → merge_append G(cat=a,60). All 5 rows `{10,20,30,40,60}` survive both Rust-writes/Java-reads (Java `IcebergGenerics`) and Java-writes/Rust-reads directions; proves merge boundary carries all Existing entries through real parquet. **Data-level interop ✅ 2026-06-11 (fixture G — multi-bin W3):** bidirectional via `dev/java-interop/run-interop-write-data.sh` (22-step chain) + `crates/iceberg/tests/interop_write_data.rs`. V2 partitioned table: 4 fast_appends (A+B+C1+C2) → dynamic `target-size-bytes = max_manifest_len*2+1` + `min-count-to-merge=2` → merge_append G(cat=a,id=60). `pack_end` with 5 manifests bins into [new],[e1,e2],[e3,e4]; both bins of size 2 satisfy min-count → 2 merged manifests (bin-count assert ≥ 2). Live set: 7 rows a-rows={10,20,30,50,60}, b-rows={40,55}. Proves multi-bin merge path; partition column pinned (S3 lesson). Sabotage battery: truncate + bogus-path both fail closed. |
| Write: `OverwriteFiles` | 🟡 | `api/OverwriteFiles.java`, `core/BaseOverwriteFiles.java`, `core/MergingSnapshotProducer.java` | `transaction/overwrite_files.rs` — explicit add + delete in one snapshot, dynamic `operation()`, `overwrite_by_row_filter` (residual-based KEEP/DELETE/PARTIAL-error per file), conflict validations: `validate_no_conflicting_data` (filter-based), `validate_added_files_match_overwrite_filter`, `validateNewDeletes` branches A (row-filter, landed 2026-06-10) + B. Known SAFE divergence: equality-delete applicability over-approximates (over-rejects, never under). Delete-manifest carry fixed 2026-06-10 — outstanding MoR deletes survive the commit (`existing_manifest` now returns the FULL manifest list via the shared `SnapshotProducer::current_manifests`; crown-jewel scan + structural-count pins, mutation-verified).  **Metadata-level interop ✅ 2026-06-10 (explicit-API paths):** the canonical snapshot-metadata view of the Rust action matches Java's byte-for-byte in the write-actions chain (5 commits at proof time; extended to the 8-step chain 2026-06-10), judged by Java itself (`dev/java-interop/run-interop-write-actions.sh` + `crates/iceberg/tests/interop_write_actions_meta.rs`); row-filter/conflict-validation/multi-spec paths NOT covered by the chain. **Stale deferral corrected 2026-06-10:** Java has NO `validateDataFilesExist` block in `BaseOverwriteFiles.validate` (L135-175 — three blocks, all ported; `validateDataFilesExist` is RowDelta-only and already landed there); OverwriteFiles' concurrent-removal protection is `failMissingDeletePaths`, mirrored by `resolve_delete_paths`. **Data-level interop ✅ 2026-06-11 (fixture C — W1):** bidirectional via `dev/java-interop/run-interop-write-data.sh` (steps 6+10 in the 12-step chain) + `crates/iceberg/tests/interop_write_data.rs`. V2 partitioned table (identity(category), schema `{id,category,data}`): fast_append A(cat=a,10/20/30)+B(cat=b,40) → overwrite_files DELETE B ADD B'(cat=b,41,d'). Live rows `{(10,a),(20,b),(30,c),(41,d')}` (id=40 absent) in both Rust-writes/Java-reads (Java `IcebergGenerics`) and Java-writes/Rust-reads directions; partition column pinned (S3 partition-projection lesson). row-filter/conflict-validation/multi-spec paths NOT covered. Narrative: [archive](archive/2026-06_matrix-cell-narratives.md) (cell-split repair 2026-06-10 — the de-triplication move had split this row's narrative on a logical-OR inside a code snippet; the strand is rejoined in the archive). |
| Write: `ReplacePartitions` (dynamic/static overwrite) | 🟡 | `api/ReplacePartitions.java`, `core/BaseReplacePartitions.java`, `core/MergingSnapshotProducer.java`, `core/ManifestFilterManager.java` | `transaction/replace_partitions.rs` — DYNAMIC partition overwrite: replaces every partition an added file belongs to via `SnapshotProducer::resolve_partition_deletes` feeding the shared rewrite path; full replace on an unpartitioned table; `replace-partitions` marker; opt-in `validate_no_conflicting_data`. Delete-manifest carry fixed 2026-06-10 — outstanding MoR deletes in UNREPLACED partitions survive the commit (`existing_manifest` now returns the FULL manifest list via the shared `SnapshotProducer::current_manifests`; crown-jewel scan + structural-count pins, mutation-verified).  **Metadata-level interop ✅ 2026-06-10 (explicit-API paths):** the canonical snapshot-metadata view of the Rust action matches Java's byte-for-byte in the write-actions chain (5 commits at proof time; extended to the 8-step chain 2026-06-10), judged by Java itself (`dev/java-interop/run-interop-write-actions.sh` + `crates/iceberg/tests/interop_write_actions_meta.rs`); row-filter/conflict-validation/multi-spec paths NOT covered by the chain. Deferred (→ ✅ beyond the chain): static `replaceByRowFilter`/explicit-partition APIs. Narrative: [archive](archive/2026-06_matrix-cell-narratives.md). **Data-level interop ✅ 2026-06-11 (fixture E — W2):** bidirectional via `dev/java-interop/run-interop-write-data.sh` (18-step chain, steps 8+12) + `crates/iceberg/tests/interop_write_data.rs`. V2 partitioned table (identity(category), schema `{id,category,data}`): fast_append A(cat=a,10/20/30)+B(cat=b,40) → replace_partitions E_new(cat=a,id=11,data="a'"). Live rows `{(11,a'),(40,d)}` (A's rows 10/20/30 all gone) in both Rust-writes/Java-reads (Java `IcebergGenerics`) and Java-writes/Rust-reads directions; partition column pinned (S3 partition-projection lesson); EXTRA assertion: partition-b file (B) carries forward as EXISTING manifest status — not re-added as ADDED (Java oracle step 3f), proving the untouched partition's file is byte-untouched in the metadata. |
| Write: `DeleteFiles` | 🟡 | `api/DeleteFiles.java`, `core/StreamingDelete.java`, `core/ManifestFilterManager.java` | `transaction/delete_files.rs` — delete by path / `DataFile` reference, plus the foundational manifest-filter/rewrite machinery (`SnapshotProducer::process_deletes`: rewrite/keep/drop + provenance preservation) every delete-bearing action reuses; opt-in `validate_files_exist` (the status-axis walk, 2026-06-09). Delete-manifest carry fixed 2026-06-10 — outstanding MoR deletes survive the commit (`existing_manifest` now returns the FULL manifest list via the shared `SnapshotProducer::current_manifests`; crown-jewel scan + structural-count pins, mutation-verified).  **Metadata-level interop ✅ 2026-06-10 (explicit-API paths):** the canonical snapshot-metadata view of the Rust action matches Java's byte-for-byte in the write-actions chain (5 commits at proof time; extended to the 8-step chain 2026-06-10), judged by Java itself (`dev/java-interop/run-interop-write-actions.sh` + `crates/iceberg/tests/interop_write_actions_meta.rs`); row-filter/conflict-validation/multi-spec paths NOT covered by the chain. **Data-level interop ✅ 2026-06-11 (fixture D — W1):** bidirectional via `dev/java-interop/run-interop-write-data.sh` (steps 7+10 in the 12-step chain) + `crates/iceberg/tests/interop_write_data.rs`. V2 partitioned table (identity(category), schema `{id,category,data}`): fast_append A(cat=a,10/20/30)+B(cat=b,40)+C_file(cat=a,50,e) → delete_files {B} by path. Live rows `{(10,a),(20,b),(30,c),(50,e)}` (id=40 absent) in both Rust-writes/Java-reads (Java `IcebergGenerics`) and Java-writes/Rust-reads directions; partition column pinned (S3 partition-projection lesson). Deferred (→ ✅ beyond the chain): delete-by-row-filter/partition-predicate. Narrative: [archive](archive/2026-06_matrix-cell-narratives.md). |
| Write: `RowDelta` (merge-on-read) | 🟡 | `api/RowDelta.java`, `core/BaseRowDelta.java`, `core/MergingSnapshotProducer.java` | `transaction/row_delta.rs` — the merge-on-read commit: data files + position/equality DELETE files in ONE snapshot; producer delete-manifest support; added delete entries inherit the new snapshot's seq (the load-bearing invariant); dynamic `operation()`; conflict validations `validate_no_conflicting_data_files`/`_delete_files`, `validate_data_files_exist` (+ skip-deletes op-set), `validate_no_new_deletes_for_data_files` (validation-only). The full write→read chain is proven, and the DATA level is **interop ✅ both directions 2026-06-09** (see the merge-on-read read row). **Metadata-level interop ✅ 2026-06-10** — the canonical snapshot-metadata view (operation classification, count summaries, data/delete manifest split, post-inheritance sequence numbers) of Rust-written `fast_append`+`row_delta` chains matches Java's BYTE-FOR-BYTE across all three scan-exec fixtures, judged by Java itself (`dev/java-interop/run-interop-rowdelta-meta.sh` + `crates/iceberg/tests/interop_rowdelta_meta.rs`); the harness surfaced + fixed the `changed-partition-count` summary parity bug (`spec/snapshot_summary.rs`, 2026-06-10). **D3 (2026-06-10):** deletion-vector adds commit (format-version gate: V1 throws / V2 rejects DVs / V3 requires DVs for position deletes, eq exempt — Java-exact messages, 1.10.0-bytecode-verified; the existing always-on `validateAddedDVs` walk widened to Java's `{OVERWRITE, DELETE, REPLACE}` op set); always-on `validateNoConflictingFileAndPositionDeletes` added (1.10.0-bytecode-verified member of `BaseRowDelta.validate` that was missing); Rust-conservative fresh-DV-only door (see the DV-writer row). **D4 (2026-06-10): the DV commit is interop-proven** — TABLE level (Java's production scan reads a Rust `row_delta` DV commit on V3) AND metadata level (canonical view byte-identical 3 ways: `added-dvs`, operation `delete`, manifest/seq semantics) — `run-interop-dv.sh` steps 7-11 + `tests/interop_dv_table.rs`; details on the DV-writer row. **Arc-E Inc 1 (2026-06-10): apply-side `removeDeletes` LANDED** — `remove_deletes(DeleteFile)` actually drops a superseded delete file (the producer's `removed_delete_files` → resolve-by-path → `process_deletes` tombstones it in the rewritten DELETE manifest via the content-keyed filtering writer; summary `remove_file` → `removed-dvs`/`removed-position-delete-files`/`removed-equality-delete-files`); the fresh-DV door gained the `remove_deletes` escape hatch (a DV may shadow a live delete IFF that delete is removed in the same commit); crown jewel = all-Rust DV-replaces-DV (DV1 {1} → add DV2 {1,3} + remove DV1 → one live DV, old tombstoned, `removed-dvs:1`+`added-dvs:1`). **OPERATION CLASSIFICATION FIXED to 1.10.0 bytecode** — `operation()` is the two-branch `addsDeleteFiles && !addsDataFiles ⇒ DELETE; else ⇒ OVERWRITE` (NO APPEND branch in 1.10.0; MAIN's append arm is post-1.10.0 — the 2026-06-08 "third condition" lesson, settled against the jar). **`removeRows` apply-side removal LANDED 2026-06-11 (multispec-closeout 3):** the removed DATA-file paths route through the producer (`RowDeltaOperation::delete_files` → the shared `resolve_delete_paths` → `process_deletes` rewrite + summary `remove_file`) EXACTLY as `OverwriteFiles`'/`DeleteFiles`' removal does, so a removed data file drops from the scan in the SAME row-delta snapshot; `operation()` UNAFFECTED (1.10.0 two-branch form consults neither `deletesDataFiles()` nor the removal set — confirmed); the removed∩referenced rejection fires FIRST (in `validate()`, before `commit()`); 6 tests (drops-from-scan e2e + producer-routing-sever mutation, remove-only⇒Overwrite, missing-path fail-loud, ordering pin, summary counters, replace-in-place remove+add-same-path). DOCUMENTED DIVERGENCE: the missing-removal-path fail-loud is STRICTER than Java's `RowDelta` default (Java's `BaseRowDelta` does NOT set `failMissingDeletePaths` for `removeRows` — its only call sits behind `if (validateDeletes)`; `StreamingDelete`/`BaseOverwriteFiles` DO set it, so `resolve_delete_paths`' unconditional fail-loud is Java-faithful for THOSE callers only — 1.10.0-bytecode-verified; the same loud posture as `removeDeletes` from Arc E, documented on `resolve_delete_paths`). **Arc-E Inc 2 (2026-06-10): the writer-side DV merge + the engine `add_deletes(merged).remove_deletes_many(rewritten)` replacement flow are now interop-proven both directions** (`tests/interop_dv_replace.rs` — see the DV-writer row). RowDelta stays 🟡 for its own residue (multi-spec delete commits; full conflict-validation interop). Narrative: [archive](archive/2026-06_matrix-cell-narratives.md). |
| Write: `RewriteFiles` (compaction commit) | 🟡 | `api/RewriteFiles.java`, `core/BaseRewriteFiles.java`, `core/MergingSnapshotProducer.java`, `core/ManifestFilterManager.java` | `transaction/rewrite_files.rs` — atomically replace DATA files in one `Operation::Replace` snapshot (Java `BaseRewriteFiles`); non-empty-delete precondition; reuses the shared by-path rewrite machinery; carries DELETE manifests forward unchanged. **`dataSequenceNumber` preservation LANDED 2026-06-10:** `data_sequence_number(seq)` stamps the added files with the replaced files' data seq so outstanding equality deletes still apply (`data_seq < delete_seq`); negative seq rejected (`DataInvalid` — writer silently strips it into re-inheritance). **Guard LIFTED 2026-06-10:** the old hard rejection of tables with outstanding delete manifests is gone (Java-identical — Java has no such guard; without `data_sequence_number` the caller owns the resurrection hazard). **`validateNoNewDeletes` LANDED 2026-06-10:** `validate_from_snapshot` + `validate` (Java `BaseRewriteFiles.validate`) delegate to the shared `validate_no_new_deletes_for_data_files` with `ignore_equality_deletes = data_sequence_number.is_some()` (Java L475-479) — concurrent position deletes always fatal, equality deletes ignored when seq preserved. Crown-jewel eq-delete resurrection pin (real parquet + eq-delete writer + scan + on-disk seq, seq-strip-mutation-verified) + no-override tx-captured-start pin (refreshed-head-mutation-verified). **Metadata-level interop ✅ 2026-06-10 (seq-preservation + delete-carry paths):** the canonical snapshot-metadata view matches Java's byte-for-byte both in the eight-step write-actions chain AND in a dedicated delete-bearing fixture B (`run-interop-write-actions.sh` + `interop_write_actions_meta.rs`): fixture B pins that `data_sequence_number(1)` makes A' carry data-seq 1 (not the rewrite snapshot's seq 3) in the post-inheritance view AND that the outstanding position-delete manifest survives the rewrite intact — Java's `rewriteFiles(.., 1L)` + `validateFromSnapshot`, mirrored by the Rust tx-captured start (the semantic twin). **Empirical 1.10.0 probe:** rewriting the data file a delete REFERENCES (so the delete dangles) commits and KEEPS the dangling parquet position-delete on Java — CONVERGES with Rust's carry-unchanged posture (parity), so it is left out of the byte-diffed chain. (Mechanism re-traced 2026-06-10: 1.10.0 prunes only dangling **DVs** on a `RewriteFiles` — `isDanglingDV` is gated on `FileFormat.PUFFIN`, so a V2 position-delete parquet is structurally exempt, and the `minSequenceNumber` cutoff also misses it. The `removeDanglingDeletesFor` `UnsupportedOperationException` at `MergingSnapshotProducer` L1220-1222 is on the inner DATA-file filter manager and is NOT the path invoked for delete pruning — the earlier "BaseRewriteFiles overrides it to throw" attribution was incorrect.) Multi-spec / conflict-validation paths NOT covered. **DELETE-file REMOVAL surface LANDED 2026-06-11 (O3):** `delete_delete_file(DeleteFile)`/`delete_delete_files` (Java `RewriteFiles.deleteFile(DeleteFile)` → `MergingSnapshotProducer.delete(DeleteFile)`) route the removed delete files through the producer's existing apply-side removal (`with_removed_delete_files` → `resolve_delete_file_paths` by path against the DELETE manifests, fail-loud on a missing path → `process_deletes` tombstone in the content-keyed rewritten DELETE manifest → summary `removed-position-delete-files`/`removed-equality-delete-files`/`removed-dvs`); operation stays `Replace` (Java `operation()` = "replace" unconditionally, 1.10.0 bytecode); ALL THREE preconditions now enforced with Java-exact messages (`validateReplacedAndAddedFiles()` disassembled — (1) "Files to delete cannot be empty" relaxed to data OR delete, (2) "Data files to add must be empty…", (3) "Delete files to add must be empty because there's no delete file to be rewritten" — the matrix's named third precondition; (3) is presently unreachable since the add-delete surface is not ported, kept for when it lands); content-type guard rejects a `Data` file on the removal path. This is the commit vehicle `RemoveDanglingDeleteFiles` drives (5 tests: delete-only⇒Replace+counter, position-delete removal restores rows, data-content rejection, missing-path fail-loud, producer-routing tombstone mutation pin). Still deferred (→ ✅): DELETE-file ADD (`addFile(DeleteFile)`/`addFile(DeleteFile, long)` — rewriting a delete file into a NEW delete file, needs the added-delete write path); multi-spec / conflict-validation interop. Narrative: [archive](archive/2026-06_matrix-cell-narratives.md). **Data-level interop ✅ 2026-06-11 (fixture B — S1, seq-preservation):** bidirectional via `dev/java-interop/run-interop-write-data.sh` + `crates/iceberg/tests/interop_write_data.rs`. Unpartitioned 2-field table `{id,data}`: fast_append A(ids 10..50) → row_delta equality-delete(ids 20+40, seq 2) → rewrite {A}→{A'} with `data_sequence_number(1)`. Live rows `{10,30,50}` (ids 20+40 absent) in both Rust-writes/Java-reads and Java-writes/Rust-reads directions; proves seq-preservation: A'.data_seq=1 < eq_del.seq=2 so the delete still applies after the rewrite. CRITICAL DESIGN LESSON: position deletes are PATH-BASED (not seq-based); after A→A' the delete on A's path is dangling. To prove `data_sequence_number` preservation, an EQUALITY delete is required (applicability is strictly seq-based). Multi-spec / conflict-validation paths NOT covered. Still deferred (→ ✅): DELETE-file rewrite (`addFile`/`deleteFile(DeleteFile)`) + its third Java precondition. **Data-level interop ✅ 2026-06-11 (fixture F — W2, partitioned shape):** bidirectional via `dev/java-interop/run-interop-write-data.sh` (18-step chain, steps 9+12) + `crates/iceberg/tests/interop_write_data.rs`. V2 partitioned table (identity(category), schema `{id,category,data}`): fast_append A(cat=a,10/20/30)+B(cat=b,40) → row_delta eq-delete(cat=a, id=20, seq 2) → rewrite {A}→{A'} `data_sequence_number(1)`. Live rows `{(10,a),(30,c),(40,d)}` (id=20 absent) in both Rust-writes/Java-reads (Java `IcebergGenerics`) and Java-writes/Rust-reads directions; proves seq-preservation for the PARTITIONED shape: A'.data_seq=1 < eq_del.seq=2 so the delete still applies after the rewrite; partition B untouched; partition column pinned. The earlier unpartitioned fixture B (S1) plus this partitioned fixture F (W2) together cover both shapes. |
| Transaction action extension seam | 🟡 | `core/.../BaseTransaction` | `transaction/action.rs` — `TransactionAction`/`ApplyTransactionAction` + `ActionCommit` exist (trait is `pub(crate)`; we own it → make `pub` in Phase 2) |
| Write: `upgrade_format_version` action | ✅ | format-version upgrade | `transaction/upgrade_format_version.rs` (new in 0.9) |
| Multi-op transactions + optimistic-concurrency retry | 🟡 | `api/Transaction.java`, `core/SnapshotProducer.java`, `core/MergingSnapshotProducer.java` | `transaction/mod.rs` + `catalog.update_table` retry (`backon`, retryable-only) + the conflict-validation FOUNDATION (2026-06-08): tx-captured `starting_snapshot_id` surviving the re-base, the `validate` hook against the refreshed base, non-retryable `DataInvalid` conflicts, the shared `files_after` walk family. **Multi-spec writes landed 2026-06-11:** the `SnapshotProducer` now groups added DATA + DELETE files by `partition_spec_id` and writes ONE manifest per (content × spec) — Java parity with `MergingSnapshotProducer.newDataFilesBySpec`/`newDeleteFilesBySpec` + `FastAppend.newDataFilesBySpec` (was default-spec-only). `validate_added_data_files`/`validate_added_delete_files` check spec EXISTENCE (Java's exact "Cannot find partition spec %s for {data,delete} file: %s") + partition-value compat against the FILE's own spec; the summary collector partitions by each file's own spec (Java `addedFile(spec(file.specId()), file)`). 8 producer tests (two-spec data/delete manifests, unknown-spec rejection ×2, wrong-spec-type, cumulative totals, plus reviewer-added per-spec partition-SUMMARY-key pin and a V1 two-spec append probe) + the cherrypick multi-spec replay conversion; grouping-revert, validation-revert, summary-revert, file-loss, and default-spec-stamp mutations all verified. Deferred (→ ✅): remaining per-action validation blocks; the WRITER-LAYER spec threading (`DataFileWriter`/`DeletionVectorWriter` still stamp the table default spec — a single writer instance produces single-spec files, so a multi-spec commit is assembled by the CALLER passing files that already carry their specs); multi-spec Java↔Rust interop; validation against the REAL catalogs (Glue + S3 Tables). Narrative: [archive](archive/2026-06_matrix-cell-narratives.md). |
| Writer: data file | ✅ | `data/` | `writer/base_writer/data_file_writer.rs` |
| Writer: equality-delete | ✅ | `data/` | `writer/base_writer/equality_delete_writer.rs` |
| Writer: position-delete | 🟡 | `data/`, `core/.../deletes/PositionDeleteWriter.java` | `writer/base_writer/position_delete_writer.rs` — reserved field ids (2147483546/2147483545), write-as-given (sorting is the caller's job, matching Java), schema built FROM the reserved metadata-column fields. Proven Java-readable via the scan-exec Direction-2 interop (2026-06-09). Deferred: the sorting writer variant; the optional `row` column. Narrative: [archive](archive/2026-06_matrix-cell-narratives.md). |
| Writer: deletion-vector (V3 puffin DV) | ✅ | `core/.../deletes` (`BaseDVFileWriter`, `BitmapPositionDeleteIndex.serialize`) | D2 landed 2026-06-10: `delete_vector.rs` `serialize_deletion_vector_v1` (Java-faithful DENSE layout + `runLengthEncode` parity via `RoaringBitmap::optimize` + framing/CRC; hand-computed exact-byte goldens) + `writer/base_writer/deletion_vector_writer.rs` `DVFileWriter` (one Puffin per close, blob per referenced file, `createDV` L145-159 metadata; no-deletes ⇒ no file). **Direction-2 interop ✅ 2026-06-10 at the blob level**: Java's production reader (`Puffin.read` + `PositionDeleteIndex.deserialize`) decodes the Rust-written Puffin exactly, and the blobs are BYTE-IDENTICAL to Java's own serialization of the same positions — run containers included (`run-interop-dv.sh` steps 4-6, `tests/interop_dv_write.rs`). **D3 COMMIT path landed 2026-06-10:** `row_delta().add_deletes(dv)` commits DVs end-to-end (all-Rust crown jewel: real parquet → `DVFileWriter` → commit → scan returns the survivors; resurrection-mutation-verified); format-version gate `validateDeleteFileForVersion` (V1 throws / V2 rejects DVs / V3 requires DVs, eq exempt — exact Java messages, 1.10.0-bytecode-verified) in `SnapshotProducer::validate_added_delete_files`; `validateAddedDVs` walk fixed to Java's `VALIDATE_ADDED_DVS_OPERATIONS = {OVERWRITE, DELETE, REPLACE}` (REPLACE was wrongly dropped as "unrepresentable"); summary parity: `added-dvs`/`removed-dvs` counters (a DV counts INSTEAD of `added-position-delete-files`, still in `added-delete-files`/`added-position-deletes`; size = blob `content_size_in_bytes` per `ScanTaskUtil`) in `spec/snapshot_summary.rs`; committed-DV manifest write→read round-trip pinned CLEAN (fields 143-145 survive the Rust V3 writer). **Rust-conservative fresh-DV door + its `remove_deletes` escape hatch (documented divergence, relaxed Arc-E Inc 1 2026-06-10):** a DV add for a data file with a LIVE position-scoped delete that still APPLIES (existing DV, or shadowed legacy parquet position delete on an upgraded table — applicability is the read-path test against the referenced file's live DATA entry: path/(spec id, partition) scope + `delete_seq >= data_seq`, so it survives partition evolution and ignores predating deletes; review fix 2026-06-10) is rejected at commit UNLESS the same commit REMOVES that existing delete (`remove_deletes` — the apply-side removal, now landed). Java MERGES previous deletes into the new DV (`BaseDVFileWriter` L117-126) + removes the superseded file via `rewrittenDeleteFiles` → `removeDeletes`; the apply-side removal half is now ported (`removed-dvs` is reachable end-to-end — the all-Rust DV-replaces-DV crown jewel), so what remains deferred is only the WRITER-side AUTO-merge (`loadPreviousDeletes` reading + unioning the old positions). **D4 TABLE-level Direction-2 interop ✅ 2026-06-10:** Java's PRODUCTION scan (`IcebergGenerics`) read a Rust-COMMITTED V3 table — 2 identity partitions of real parquet `fast_append`ed + ONE puffin holding TWO DVs `row_delta`'d — to exactly the expected live rows, plus a manifest-API cross-check of the committed `DeleteFile` metadata (content/format/referenced-data-file/blob coordinates/cardinality + the shared-puffin multi-blob pin); dropped-DV mutation = Java-visible resurrection (`run-interop-dv.sh` steps 7-8, `tests/interop_dv_table.rs`). **D4 METADATA-level interop ✅ 2026-06-10:** the canonical snapshot-metadata view of the Rust DV chain matches Java's BYTE-FOR-BYTE all 3 ways — `added-dvs` (emitted INSTEAD of `added-position-delete-files`), operation classification `delete`, manifest split + post-inheritance seqs (steps 9-11; the view's entry tuple omits the DV-specific fields — those are covered by the manifest cross-check). **Arc-E Inc 2 (2026-06-10): the WRITER-side previous-deletes MERGE hook LANDED — the last deferred piece, row flips 🟡→✅.** `DVFileWriter::with_previous_deletes(HashMap<path, PreviousDeletes>)` mirrors Java's `loadPreviousDeletes: Function<String, PositionDeleteIndex>` ctor arg; `close_with_result()` returns a `DVWriteResult { delete_files, rewritten_delete_files }` (the Rust `DeleteWriteResult`): previous positions are unioned into the new DV (`DeleteVector::merge`) and every FILE-SCOPED source file is returned for removal. `is_file_scoped` mirrors `ContentFileUtil.isFileScoped` (1.10.0 bytecode: `referencedDataFile(df) != null` — equality→false, explicit `referenced_data_file`, or equal `_file_path` bounds; partition-scoped parquet deletes NOT rewritten, Java L121-124). The no-previous case is byte-identical to D2 (pinned). 7 unit tests (merge cardinality, file-scoped-vs-not selectivity, eq-delete exclusion, unwritten-path ignore, byte-identical floor, `is_file_scoped` predicate, the all-Rust crown jewel: DV1{1} → load back via the production decoder → writer merges {1,3} → `add_deletes(merged).remove_deletes_many(rewritten)` → scan {10,30,50}); merge no-op + `is_file_scoped`-always-true mutations verified. **Replacement interop ✅ BOTH directions 2026-06-10** (`run-interop-dv.sh` steps 12-16, `tests/interop_dv_replace.rs` + `DvReplaceOracle`): the Rust writer-merged replacement chain (fast_append + DV1 + add merged DV2 + remove DV1) read back by Java's production scan (merged DV applied, DV1 ABSENT from the manifests — Java cross-check) AND the canonical metadata views match BYTE-FOR-BYTE all 3 ways — the FIRST LIVE `removed-dvs` comparison. **Run-store byte question answered (the D2 tie caveat, now live):** the merged blob (previous {1} ∪ new {3} = {1,3}, array container) is BYTE-IDENTICAL to Java's same merge; the documented universal caveat stands (a previous DV whose store is ALREADY a Run container re-serializes at `cardinality == 2·runs` to an array on `roaring-rs` vs a run on Java — positions identical, bytes may differ, Java reads our blob either way — a byte-only divergence, not contorted around in production). Residue moving ELSEWHERE: the READ row keeps its own residue (duplicate-DV-per-file rejected at the load door not the index; `dv.dataSequenceNumber() >= seq` deferred). API-NOTE: making the public hook take a `DeleteVector` required `pub mod delete_vector` (the on-disk DV type is now nameable downstream). Narrative: [archive](archive/2026-06_matrix-cell-narratives.md). |
| Writer: partitioning (fanout/clustered/unpartitioned) | ✅ | — | `writer/partitioning/` |
| Read: Parquet → Arrow | ✅ | `parquet/` | `arrow/reader.rs` |
| Read: merge-on-read apply (position-deletes + DVs during scan) | 🟡 | `data/.../DeleteFilter`, `data/.../BaseDeleteLoader`, `core/DeleteFileIndex.java` | `arrow/delete_filter.rs`, `arrow/caching_delete_file_loader.rs`, `delete_file_index.rs`, `delete_vector.rs`. **Parquet position/equality deletes: data-level interop ✅ 2026-06-09 — the full cross-product** {position, equality} × {Java-writes-Rust-reads, Rust-writes-Java-reads} × {unpartitioned, partitioned}, via `crates/iceberg/tests/interop_scan_exec.rs` + run scripts (mutation-pinned). **HONESTY CORRECTION 2026-06-10: the previous ✅ over-claimed DVs** — it came from the 0.9.1 sync notes and was never scan-verified; until D1 a Puffin DV was misrouted to the parquet reader (the DV loader was a TODO) and a V3+DV scan FAILED. **D1 (2026-06-10) added the DV scan READ path:** `deletion-vector-v1` blob decode (BE length / `D1 D3 39 64` magic / BE CRC-32 / portable 64-bit roaring, Java `BitmapPositionDeleteIndex.deserialize` + `RoaringPositionBitmap.deserialize` validations, fuzz-shaped negative tests), single-ranged-read loading per Java `BaseDeleteLoader.readDV` keyed by `referenced_data_file`, and Java `DeleteFileIndex.forDataFile` precedence (a file's DV supersedes its parquet position deletes; eq deletes still apply). **Direction-1 interop ✅ 2026-06-10** (`run-interop-dv.sh` + `tests/interop_dv_scan.rs` + the raw-blob decode pin incl. >2^32 positions + run containers): Rust scans a Java-1.10.0-written V3 table with a real `BaseDVFileWriter` DV. Residue (the row stays 🟡): Java's two index-level `ValidationException`s are not index-level here — duplicate-DV-per-file is rejected at the load door instead (`DeleteFileIndex.java` L528-535); `dv.dataSequenceNumber() >= seq` (Java `findDV` L208-214, an invalid-table state) now LANDED 2026-06-11 (multispec-closeout 3) — the index was made FALLIBLE (one production caller, `scan/context.rs`, already `Result`-returning; the index is the only place both sequence numbers are in hand, vs the load door which receives neither — `FileScanTaskDeleteFile` drops them) and a `dv_seq < data_seq` lookup returns the EXACT 1.10.0 message "DV data sequence number (%s) must be greater than or equal to data file sequence number (%s)" (hand-built invalid metadata test + check-disabled mutation; valid boundary dv_seq==data_seq / dv_seq>data_seq still applies the DV); deferred from 2026-06-09: multi-file-per-partition, non-identity transforms, more column types. **DVs data-level interop ✅ BOTH directions 2026-06-10:** Direction 1 = D1 above; Direction 2 = D4's table-level proof — Java's production scan reads a Rust-COMMITTED V3 table whose merge-on-read deletes are two Rust-written DVs in one puffin (see the DV-writer row; `run-interop-dv.sh` + `tests/interop_dv_table.rs`). Narrative: [archive](archive/2026-06_matrix-cell-narratives.md). |
| Read: ORC data files | ❌ | `orc/` | none |
| Read/write: Avro data files | ❌ | `core/.../avro` (data) | Avro is manifest-only here |
| Scan planning + partition pruning | ✅ | `api/TableScan.java` | `scan/` — manifest-list → manifest → file pruning (manifest evaluator, partition filter, metrics evaluator), `use_ref` branch/tag scanning (2026-06-08), per-task PARTITION-REDUCED residuals (2026-06-08). **Scan-PLANNING interop ✅ 2026-06-09 (A4):** planned file set + per-file delete association + residual-always-true bit match Java; full residual-expression string comparison deferred (cross-language syntax). Narrative: [archive](archive/2026-06_matrix-cell-narratives.md). |
| Metrics evaluators (inclusive/strict) + residual evaluation | 🟡 | `expressions/`, `api/.../expressions/ResidualEvaluator.java` | `expr/visitors/` — inclusive/strict metrics evaluators + `ResidualEvaluator` (core 2026-06-08 + scan wiring: each `FileScanTask` carries the partition-reduced residual). Identity-partition constant materialization (Java `PartitionUtil.constantsMap`) ACTIVATED 2026-06-11: `context.rs` threads each manifest's `partition_spec` onto its tasks; `record_batch_transformer` materializes identity-partition columns as PLAIN arrays of the declared scan-schema type (the prior REE-leak bug fixed) coerced to the column's Iceberg type via `Datum::to` (int→long widening fixed), and forces the constant to OVERRIDE a file-present column (verified against Java 1.10.0 bytecode: `BaseParquetReaders.ReadBuilder.struct` → `ParquetValueReaders.replaceWithMetadataReader` returns the constant reader unconditionally when `idToConstant` has the field id, discarding the file-column reader — constant wins by construction). BEHAVIOR CHANGE: any identity-partitioned table whose data-file values diverge from partition metadata now returns the PARTITION value (was the file value); for spec-consistent tables (the norm) results are unchanged. Multi-spec: each file's constants use its own spec. Gated on `cargo test -p iceberg-datafusion` (lib + MemoryCatalog integration incl. `test_insert_into_partitioned`). Unit + scan tests ship; data-level Java interop round-trip still pending (keeps 🟡). Narrative: [archive](archive/2026-06_matrix-cell-narratives.md). |
| `IncrementalAppendScan` | 🟡 | `api/IncrementalAppendScan.java`, `core/BaseIncrementalAppendScan.java`, `core/BaseIncrementalScan.java` | `scan/incremental.rs` (2026-06-08) — exclusive/inclusive `from` ranges, append-only snapshot walk, own-added-manifest + `Added`-entry filters, reuses the normal scan's plan machinery via the `_from_files` seam. Deferred (→ ✅): Java interop. Narrative: [archive](archive/2026-06_matrix-cell-narratives.md). |
| `IncrementalChangelogScan` | 🟡 | `api/IncrementalChangelogScan.java`, `core/BaseIncrementalChangelogScan.java`, `core/BaseIncrementalScan.java` | `scan/incremental.rs` (2026-06-08) — Insert/Delete changelog tasks with OLDEST→0 ordinals, deletes sourced from the deleting snapshot's own rewritten manifests; delegates range resolution to the append-scan builder. Deferred: `BatchScan`, CDC-merge (`UPDATE_BEFORE`/`UPDATE_AFTER`), interop. Narrative: [archive](archive/2026-06_matrix-cell-narratives.md). |
| `BatchScan` | ❌ | `api/BatchScan.java` | none |
| Scan/commit metrics reporting (`ScanReport`, `MetricsReporter`) | 🟡 | `metrics/` | `metrics/mod.rs` (data model, 2026-06-08: Java-shape serde incl. counter/timer wire format) + OPT-IN scan emission wiring (2026-06-09: `with_metrics_reporter`, report ONCE on full stream consumption; no-reporter path byte-unchanged). Documented divergence: the `filter` field uses Rust `Predicate` serde, not Java `ExpressionParser` JSON (large separate port). Deferred: `LoggingMetricsReporter` (needs a logging-facade dep approval); the 6 not-yet-collectable counters stay `None`. Narrative: [archive](archive/2026-06_matrix-cell-narratives.md). |
| Catalogs: REST, Hive, Glue, S3 Tables, SQL/JDBC, in-memory | ✅ | `core/.../{rest,jdbc,inmemory}`, `aws`, `hive-metastore` | `crates/catalog/*`, `catalog/memory` |
| `ViewCatalog` + view operations (create/replace/drop/list, versions) | 🟡 | `api/catalog/ViewCatalog.java`, `api/view/`, `core/view/` | view metadata spec + builder (`spec/view_metadata*`, `view_version.rs`) is a 1:1 port of Java `ViewMetadata.Builder` (version-id assignment + identical-version REUSE, schema interning, version-log append/expiry, dialect rules). **U1 (2026-06-12):** `ViewMetadata::read_from`/`write_to`; `view.rs` — `View` handle, `ViewCommit` (mirrors `TableCommit`), the `replace_version`/`update_properties` pending-update ops (Java `ViewVersionReplace`/`PropertiesUpdate`); `ViewRequirement`+`ViewUpdate::apply` in `catalog/mod.rs`; `Catalog` trait view surface (`create_view`/`load_view`/`drop_view`/`view_exists`/`rename_view`/`list_views`/`update_view`, default-erroring) + full MemoryCatalog impl (separate `view_metadata_locations` namespace state). Replace requirement set = `[AssertViewUUID]` ALONE, bytecode-pinned from `UpdateRequirements.forReplaceView` (no AddViewVersion arm; AddSchema arm no-ops on null base). Java-wire FIELD-SET round-trip + version-reuse + lifecycle e2e (create→replace→rename→drop) pinned. Table↔view name-space collisions now rejected both directions (U1 reviewer 2026-06-12, matches Java InMemoryCatalog). KNOWN GAP (in-tree MemoryCatalog only): `update_view` has no base-location CAS (a stale concurrent commit lands last-write-win; same gap as the pre-existing `update_table`) — needs a dedicated concurrency-parity increment. **U2 (2026-06-12):** SQL catalog (`crates/catalog/sql`) full view CRUD — views share `iceberg_tables` discriminated by `iceberg_type = 'VIEW'` (Java `JdbcUtil.VIEW_RECORD_TYPE`, V1-schema exact-match, bytecode-cited), all 7 methods (`create`/`load`/`list`/`drop`/`rename`/`view_exists`/`update_view`) mirroring the JDBC view SQL constants + table↔view collision guards both directions (Java `JdbcViewOperations.doCommit`/`ViewAwareTableBuilder`). The SQL `update_view` IMPLEMENTS the JDBC location-CAS (`UPDATE ... AND metadata_location = ?` → 0 rows = `CatalogCommitConflicts`, Java `V1_DO_COMMIT_VIEW_SQL`/`CommitFailedException`) — per-catalog, so it diverges deliberately from MemoryCatalog's no-CAS posture (concurrent-race test pins the loser fails). REST catalog (`crates/catalog/rest`): typed view shapes (`CreateViewRequest`/`LoadViewResult`/`CommitViewRequest` — Java wire `name`/`location`/`view-version`/`schema`/`properties`, `metadata-location`/`metadata`/`config`, and the `UpdateTableRequest`-shape commit) with serde round-trips vs hand-pinned JSON, PLUS all 7 view methods fully wired to the OpenAPI routes (`GET/POST /namespaces/{ns}/views`, `GET/POST/DELETE/HEAD .../views/{view}`, `POST /views/rename`) — mockito shape-tests pin each route + status mapping (no live server). 10 SQL + 10 REST view tests. Deferred: Glue/S3Tables views (credentialed sprint), view interop (next wave; Rust↔Java field-set reads already work, byte order cosmetic), `SessionCatalog`/`LockManager` (separate). |
| `SessionCatalog` | ❌ | `api/catalog/SessionCatalog.java` | none |
| `LockManager` | 🟡 | `api/LockManager.java` | partial |
| Encryption (`EncryptionManager`, KMS, encrypted FileIO/manifests) | ❌ | `api/encryption/`, `core/.../encryption` | V3 `spec/encrypted_key.rs` stub only |
| FileIO (S3/GCS/Azure/OSS/fs/memory) | ✅ | `core/.../io`, cloud modules | `io/` + extracted `crates/storage/opendal` (OpenDAL) |
| Puffin read/write + blob types (theta NDV, DV) | 🟡 | `core/.../puffin`, `api/.../puffin` | `puffin/` (blob coverage partial) |
| Maintenance: `ExpireSnapshots` | 🟡 | `api/ExpireSnapshots.java`, `core/RemoveSnapshots.java`, `core/ReachableFileCleanup.java` | `transaction/expire_snapshots.rs` (2026-06-11, B1 — metadata retention, 1.10.0 bytecode-verified) + `transaction/expire_cleanup.rs` (2026-06-11, B2 — `ReachableFileCleanup` file cleanup: post-commit-only `ExpireSnapshotsCleanup::commit_and_clean` seam, candidates-minus-retained set algebra by path, live-entry content-file subtraction across data AND delete manifests incl. shared-puffin DVs, statistics files, injectable delete fn, failures collected in `CleanupReport`). **A3 INTEROP (2026-06-11) — deferral RESOLVED for retention + `ReachableFileCleanup`, BOTH directions** (`dev/java-interop/run-interop-expire.sh` + `crates/iceberg/tests/interop_expire.rs`, 5 fixtures: linear/tag_protected/stats/deletes/rewrite): Java's `expireSnapshots().cleanExpiredFiles(true).deleteWith(collector)` vs Rust `ExpireSnapshotsAction` + `ExpireSnapshotsCleanup::commit_and_clean`; D1 (Rust acts, Java judges) byte-equal canonical views + Java-verified surviving snapshots/refs + deleted set; D2 (Java acts, Rust verifies) Rust view == Java view + Rust cleanup deleted set == Java `deleteWith` set (path-independent `<funnel>@ord<N>#<discriminator>` descriptor — injective per-file via record-count / file-counts / size, reviewer-hardened). Java forced down `ReachableFileCleanup` (the only ported strategy) for EVERY fixture via the real engine selector `RemoveSnapshots.withIncrementalCleanup(false)` (reviewer fix: a HEAD tag alone leaves Java auto-selecting `IncrementalFileCleanup` because `hasNonMainSnapshots(current)` needs an off-main survivor; bytecode-cited). Documented divergences: cleanup is explicit opt-in (Java defaults `cleanExpiredFiles(true)`); retained-shared manifest lists spared (under-deletion, pinned). Deferred: `IncrementalFileCleanup` (optimization with stricter eligibility), `cleanExpiredMetadata`, ref-age (`max_ref_age_ms`) interop. STOP-FINDING (raised here, FIXED in a separate increment): Rust `FastAppend::existing_manifest` DROPPED all-tombstone manifests where Java `FastAppend.apply` carries ALL prior manifests forward — fixtured away in A3 (the `rewrite` fixture's delete leaves a file existing); **FIXED 2026-06-11 in Wave-4 Group O / O1 — see the fast-append row.** |
| Maintenance: `DeleteOrphanFiles` | 🟡 | `api/actions/DeleteOrphanFiles.java`, `spark/.../DeleteOrphanFilesSparkAction.java`, `core/ReachableFileUtil.java`, `core/util/FileSystemWalker.java`, `core/actions/FileURI.java`, `core/hadoop/HiddenPathFilter.java` | `maintenance/delete_orphan_files.rs` (2026-06-11, A2): builder action over `FileIO::list` (A1). Valid-file universe = ALL snapshots' content files (every entry incl. DELETED tombstones, Java `ManifestFiles.read` not `liveEntries`) + all manifests + all manifest lists + current/previous metadata.json (non-recursive) + version-hint + statistics/partition-statistics. Hidden-path filter (`_`/`.` except `_<partition-field>=`), `olderThan` cut (default now−3d), URI-normalized orphan join (scheme-less local path matches any actual scheme per Java `uriComponentMatch`), `PrefixMismatchMode {Error,Ignore,Delete}`, `equal_schemes` (defaults `{s3n,s3a→s3}` merged) / `equal_authorities`, GC gate, delete failures collected. CAVEAT: action is MAIN-source (no 1.10.0 Spark bytecode); helpers (`FileURI`/`PrefixMismatchMode`/`HiddenPathFilter`/`PartitionAwareHiddenPathFilter`) bytecode-verified vs core/api 1.10.0. Deferred: executor parallelism (sequential sweep), bulk deletes, `compareToFileList`/streaming, interop. |
| Maintenance: `RewriteDataFiles` (compaction) | 🟡 | `api/actions/RewriteDataFiles.java`, `core/actions/{SizeBasedFileRewritePlanner,BinPackRewriteFilePlanner,RewriteDataFilesCommitManager}.java` | `maintenance/rewrite_data_files.rs` (2026-06-11, O2): `RewriteDataFiles::new(table)` bin-pack action over the seq-preserving `RewriteFiles` commit. Planning (1.10.0): scan tasks grouped PER PARTITION (`groupByPartition` — current-spec partition else empty bucket), candidate filter `outsideDesiredFileSizeRange (length<min OR length>max) OR tooManyDeletes (deletes>=delete_file_threshold)`, forward greedy `pack` (lookback-1, `<= max_file_group_size_bytes`), group filter `enoughInputFiles (size>1 && size>=min_input_files) OR enoughContent (size>1 && inputSize>target) OR tooMuchContent (inputSize>max) OR anyMatch(tooManyDeletes)`. Reads each group's LIVE rows (merge-on-read deletes APPLIED, like the Spark runner) and rolls output at target size. `.filter(Predicate)` is FILE-SELECTION ONLY: the per-file RESIDUAL is STRIPPED before the rewrite read (O2 reviewer fix, the Rust analogue of Java's `BinPackRewriteFilePlanner.planFileGroups` `.ignoreResiduals()` + the Spark runner reading the group with NO row filter) so a file partially matching the filter keeps ALL its live rows — a leaked residual would silently DROP the non-matching rows (data loss). Defaults (bytecode-cited): `use_starting_sequence_number=true`, `min_input_files=5`, min=0.75·target, max=1.8·target, target=`write.target-file-size-bytes` (512MiB), `delete_file_threshold` disabled (`usize::MAX`), `max_file_group_size_bytes=100GiB`. SEQ RULE: when `use_starting_sequence_number`, added files stamped with the STARTING snapshot's seq (`RewriteDataFilesCommitManager.commitFileGroups` bytecode: `dataSequenceNumber(table.snapshot(startingSnapshotId).sequenceNumber())`) so a concurrent equality delete still applies. Empty plan = no-op zero-count result, NO commit. Builder: `.target_file_size_bytes/.min_input_files/.min_file_size_bytes/.max_file_size_bytes/.delete_file_threshold/.max_file_group_size_bytes/.use_starting_sequence_number/.filter(Predicate)`; `.execute(catalog)`. Result: `added_data_files_count/rewritten_data_files_count/rewritten_bytes_count/file_groups`. Deferred: partial progress (`PARTIAL_PROGRESS_*`), concurrency (`MAX_CONCURRENT_FILE_GROUP_REWRITES`/`executeWith`), sort/zorder strategies, `delete_ratio_threshold`/`tooHighDeleteRatio`, `output_spec_id`/`rewrite_all`/`rewrite_job_order`/`max_files_to_rewrite`, oversized-file input SPLITTING (Java's planner does NOT split input files — bin-packs whole tasks; output rolling only), Java interop. CAVEAT: the runner read-semantics (`SparkBinPackFileRewriteRunner`) is MAIN-source (no core/api bytecode); the planner predicate/defaults are MAIN with the literal constants bytecode-confirmed; the seq + result shape are api/core bytecode. The fork's `bin_packing` module (`transaction/merge_append.rs`) was NOT reused (it is `pub(crate)`-private to that module; opening it = a `transaction/` change, out of scope) — the lookback-1 forward `pack` is reimplemented locally, algorithm-verified against Java + the fork's packer. |
| Maintenance: `RewritePositionDeleteFiles` | ❌ | `api/actions/RewritePositionDeleteFiles.java` | none |
| Maintenance: `RemoveDanglingDeleteFiles` | 🟡 | `api/actions/RemoveDanglingDeleteFiles.java`, `spark/.../RemoveDanglingDeletesSparkAction.java` | `maintenance/remove_dangling_delete_files.rs` (2026-06-11, O3): `RemoveDanglingDeleteFiles::new(table).execute(catalog)` removes, in ONE `Replace` snapshot, every delete file in the CURRENT snapshot that can no longer apply to any live data file. DANGLING PREDICATE (derived verbatim from the Spark SQL `findDanglingDeletes`): group LIVE DATA entries by `(partition, spec_id)`, take `min(sequence_number)`; a POSITION delete dangles when `seq < min` (STRICT), an EQUALITY delete when `seq <= min` (NON-strict — the OFF-BY-ONE, complement of the read-path applicability `pos delete_seq>=data_seq` / `eq delete_seq>data_seq`), ANY delete with no live data file in its partition+spec dangles (`min IS NULL`); a DV (`findDanglingDvs`) dangles when its `referenced_data_file` is not a live data-file path. Grouping is per `(spec_id, partition)` (cross-spec isolated). Unpartitioned single-spec ⇒ early no-op (Java: ManifestFilterManager already prunes on commit). Empty plan ⇒ NO commit. VEHICLE: `RewriteFiles.deleteFile(DeleteFile)` (the delete-file-removal surface, landed this increment — see the RewriteFiles row), operation `Replace`, `removed-position-delete-files`/`removed-equality-delete-files`/`removed-dvs` summary counters. Result `removed_delete_files` (Java `Result.removedDeleteFiles()`). CAVEAT: the IMPL is SPARK MAIN-only (no 1.10.0 spark bytecode); the api interface + Result shape + the RewriteFiles vehicle/preconditions are 1.10.0 bytecode-verified. 17 tests (4 pure-fn off-by-one/no-data/cross-spec/DV-ref-gone + 8 e2e: crown-jewel still-applicable eq NOT removed, pos exact-seq boundary both ways, genuinely-dangling eq removed+counter, dangling pos-parquet removed (converges with the RewriteFiles carry-posture: plain RewriteFiles KEEPS it, this action cleans it), partition isolation, empty no-op, no-snapshot no-op, producer-routing tombstone) + 5 RewriteFiles delete-removal tests. POSTURE (reviewer-confirmed 2026-06-11, documented in the module doc): removal is METADATA-ONLY (tombstone in the rewritten DELETE manifest; bytes left for Expire/Orphan, time travel preserved); a KNOWN Java-faithful resurrection RACE exists — a delete-only `RewriteFiles` skips conflict validation (`validate` runs only when replaced-DATA is non-empty, 1.10.0 bytecode), so a concurrent seq-preserving compaction landing a lower-seq data file between plan and commit can make a removed delete applicable again (resurrection); identical in Java's action (no `validateFromSnapshot`), so pinned-to-Java not guarded; global-eq-under-multi-spec action↔reader inconsistency also inherited 1:1 from Java's `spec_id AND partition` join. Deferred: interop; DV cleanup proven via the pure-fn ref-gone test only (no e2e DV fixture); CommitManager retry/partial-progress (single-commit). |
| Maintenance: `ComputeTableStats` / `ComputePartitionStats` | 🟡 | `core/PartitionStatsHandler.java`, `core/PartitionStats.java`, `api/Partitioning.java`, `api/util/PartitionUtil.java`, `api/actions/Compute*.java` | `maintenance/partition_stats.rs` (2026-06-12, X1 — COMPUTE core only): `compute_partition_stats(table, snapshot)` ports Java 1.10.0 core `PartitionStatsHandler`'s FULL-compute aggregation. Reads `snapshot.allManifests` (the snapshot's entire manifest list, all data + delete manifests) and iterates EVERY entry LIVE + DELETED (bytecode-verified `reader.entries()`, not `liveEntries`) into a per-`(spec_id, coerced-partition)` `PartitionStats` row: data/pos/eq record + file counts, PUFFIN pos-delete → `dv_count`, `total_data_file_size_in_bytes`; `last_updated_at`/`_snapshot_id` = MAX `timestampMillis` (strict `<`, tie keeps first-seen); `total_record_count` never computed (stays null, Java-faithful); a DELETED tombstone bumps only last-updated and keeps a zero-count row. Per-manifest maps merged via `append_stats` (`dv_count` adds unconditionally per the 1.10.0 jar, not the post-1.10.0 V2 guard). `partition_stats_schema(unified_type, fv)` = the Java v2(12-field)/v3(13-field+dv_count, delete fields required) builder with the EXACT field ids 1..=13 (these become on-disk parquet ids in X2). `unified_partition_type` ports `Partitioning.partitionType`/`buildPartitionProjectionType` (one struct field per unique partition field id across all specs, deduped, newest-spec name wins, sorted ascending) + `coerce_partition` ports `PartitionUtil.coercePartition` (remap by field id, null-fill absent fields). Unpartitioned ⇒ "Table must be partitioned" Err; empty/no-manifest snapshot ⇒ empty Vec. 20 tests (schema field-id/v2-vs-v3 pins, row-type per-content-type routing + last-updated strict-tie + append-stats merge, unifier dedup + coerce both directions incl. field-id-remap, crown-jewel 2-spec/3-partition/2-snapshot hand-derived, per-content-type isolation, multi-snapshot last-updated, delete-only partition, fully-deleted-keeps-row, unpartitioned-error, empty-snapshot); 5 mutations caught (eq→data swap, drop-coercion, last-updated oldest-wins + tie-direction, skip-tombstones). **X2 (2026-06-12) — the on-disk stats FILE WRITE + registration + read-back landed.** `compute_and_write_stats_file(table, snapshot) -> Option<PartitionStatisticsFile>` ports Java 1.10.0 `computeAndWriteStatsFile`: compute (X1, already sorted by partition tuple) → empty ⇒ `Ok(None)` (Java null, no file) → write ONE parquet at `<table.location()>/metadata/partition-stats-<snapshotId>-<uuid>.parquet` (bytecode `newPartitionStatsFile` name `partition-stats-%d-%s` + `addExtension` + `metadataFileLocation`, which honors `write.metadata.path` else `<location>/metadata`), format = `write.format.default` property (default parquet; non-parquet ⇒ FeatureUnsupported — Avro/ORC writers not shipped), the field ids 1..=13 stamped on the parquet columns (the on-disk contract), return `PartitionStatisticsFile{snapshot_id, statistics_path, file_size_in_bytes=real on-disk size}`. `register_partition_stats_file(catalog, table, file)` commits a `TableUpdate::SetPartitionStatistics` + an `AssertTableUUID` requirement (Java `UpdateRequirements.forUpdateTable`) via `Catalog::update_table` — NO transaction action exists for partition statistics (`UpdateStatisticsAction` handles only `StatisticsFile`), and transaction/ was READ-ONLY, so registration uses the already-public metadata path (`TableUpdate::SetPartitionStatistics` + builder `set_partition_statistics`); a 2nd write for the same snapshot REPLACES (Java `statsToSet` map keyed by snapshot id). `read_partition_stats_file` ports `readPartitionStatsFile` + `recordToPartitionStats`: reads the whole parquet, decodes each row POSITIONALLY (idx 0 = partition struct via `arrow_struct_to_literal`, idx 1 = spec_id, 2..N = counters/nullables) → reconstructs `PartitionStats`; the decode PROJECTS the read schema to the file's actual columns (Java `InternalData.read().project()`), so a V2 file (12 cols) read against the V3 schema null-fills `dv_count`→0 and a V3 file read against the V2 schema ignores the extra column. PARTITION VALUE TYPES (X2-reviewer 2026-06-12): boolean/int/long/string/**date/timestamp/timestamptz/timestampNs/timestamptzNs/decimal/float/double** all write (date/timestamp via the shared `create_primitive_array_single_element` arrow helper, driven by the field's exact Arrow type so timezone/precision match the on-disk schema); the file's partition column is a real logical Arrow type (e.g. Date32) carrying the spec field id. +18 tests (crown-jewel write→raw-reopen field-ids-1..12 + row-count + decode → register → re-parse → read-back==computed full round-trip; v3 file has dv_count(13)+DV survives; v2 lacks 13; replace-on-rewrite + multi-snapshot coexistence; on-disk row order = sorted; empty ⇒ no file; explicit per-field-id raw stamp; record-batch round-trips every counter distinctly; register keys by file snapshot id; + date-partitioned crown-jewel (Date32 child, field id 1000, round-trip); cross-version v2-file/v3-schema null-fill + v3-file/v2-schema ignore-extra; write.metadata.path redirect; unsupported-type loud error); 4 mutation-baits caught (drop field-id stamping → raw pins fail; swap two counters at write → round-trips fail; wrong snapshot id at register → metadata pins fail; drop cross-version projection → v2-read-v3 fails; drop date logical-type arm → date crown-jewel fails; drop sort → row-order fails). **Z3 INTEROP LANDED (2026-06-12):** `dev/java-interop/run-interop-partition-stats.sh` + `tests/interop_partition_stats.rs` — BIDIRECTIONAL: D1 (Rust writes, Java `PartitionStatsHandler.readPartitionStatsFile` judges, 0 failures both chains); D2 (Java `computeAndWriteStatsFile` writes, Rust `read_partition_stats_file` reads, all rows match); cross-version V2-file/V3-schema projection (absent `dv_count` null-fills to 0). Sabotage battery 4 closed (truncate Rust parquet; SOURCE counter-cell byte-edit + re-read; truncate Java parquet; remove partition-statistics from metadata). Fixture: V2 `identity(category)`, S1 fast-append (cat=a 3rec + cat=b 2rec), S2 row-delta pos-delete (cat=a 1rec); hand-declared counters, snapshot IDs from actual written table. `PartitionStatsHandler.partition()` returns `StructLike` (not `PartitionData`) when decoded via `readPartitionStatsFile` — the oracle uses `StructLike.get(0, Object)`, not a cast. DEFERRED (named): the INCREMENTAL compute path (diff vs a prior stats file) is still open; the REMAINING partition value residue is **time/uuid/fixed/binary** (Time64 / FixedSizeBinary / LargeBinary — no `create_primitive_array_single_element` arm; error loudly, never a corrupt file); **incremental partition-value types** (uuid/time/fixed/binary) named open, not yet interop-tested. `ComputeTableStats` interop is Group Y's deferral. EXPLICITLY OUT (dependency-gated, FLAG to user): `ComputeTableStats` NDV/theta sketches need a DataSketches-equivalent crate the workspace does not carry (Cargo frozen). |
| Maintenance: `SnapshotTable` / `MigrateTable` / `RewriteTablePath` | ❌ | `api/actions/` | none |
| Partition statistics (`UpdatePartitionStatistics`, `PartitionStatisticsScan`) | ❌ | `api/Partition*Statistics*.java` | table-level stats partial |
| Table-level statistics (`UpdateStatistics`) | ✅ | `api/UpdateStatistics.java` | `transaction/update_statistics.rs` |
| Metadata inspection tables | 🟡 | `core/.../*Table` (~15 variants) | `inspect/` — the COMPLETE Java table set: `snapshots`, `manifests`, the `files` family, `entries`, `history`/`refs`/`metadata_log_entries`, `partitions`, the five `all_*` tables, and the `readable_metrics` virtual column; all SQL-queryable via `iceberg-datafusion`. **Inspection interop COMPLETE (2026-06-09→10, Direction-1 — read-only tables):** pure-metadata (offline JSON) + manifest-reading A1–A3 + scan-planning A4 + `readable_metrics` (run.sh-driven). Documented residual divergences keep the row 🟡: empty-partition column kept on unpartitioned tables (Java drops it), cross-spec partition-type unification deferred, interior readable_metrics field-id order (Java HashMap-order is non-portable; compare by name). Narrative: [archive](archive/2026-06_matrix-cell-narratives.md). |
| Name mapping (schema-less Parquet) | ✅ | `mapping/` | `spec/name_mapping/` |
| Events / listeners | ❌ | `api/events/`, `core/.../events` | none |
| Type utilities (prune/assign-ids/reassign/check-compat) | 🟡 | `types/TypeUtil.java` etc. | partial |

## Headline gaps (ranked by effort × value)

1. **Write engine** — everything beyond fast-append (`RowDelta`, `RewriteManifests`, merge append).
   **Started 2026-06-07: `DeleteFiles` 🟡 + `OverwriteFiles` 🟡 + `ReplacePartitions` 🟡 + `RewriteFiles` 🟡.**
   `DeleteFiles` delete-by-path/reference + the foundational manifest-filter / rewrite machinery in
   `SnapshotProducer::process_deletes`; `OverwriteFiles` composes that delete path with the fast-append
   add path in one `Overwrite` snapshot, reusing the now-shared `SnapshotProducer::{resolve_delete_paths,
   current_manifests}` (factored out of `DeleteFiles`; `current_manifests` carries DATA **and** DELETE
   manifests forward — Increment 2b 2026-06-10 — so outstanding MoR deletes survive every delete-bearing
   commit); `ReplacePartitions` (dynamic partition
   overwrite) adds a by-PARTITION sibling resolver `SnapshotProducer::resolve_partition_deletes` that feeds
   the SAME `process_deletes` rewrite path (every partition an added file belongs to is replaced; an
   unpartitioned table is a full replace; `replace-partitions=true` summary marker); `RewriteFiles` (the
   compaction-commit primitive) replaces a set of DATA files with a new set in one `Replace` snapshot,
   reusing the by-path `resolve_delete_paths` + `process_deletes` machinery unchanged (Java
   `BaseRewriteFiles`: `operation()` = `REPLACE`, delete set must be non-empty, `failMissingDeletePaths`).
   All four `MemoryCatalog`-tested (post-commit SCAN live set + cross-partition isolation + provenance +
   compaction-to-fewer-files). Deferred: `overwriteByRowFilter` / delete-by-row-filter / static
   `replaceByRowFilter` (metrics evaluators), DELETE-file rewrite + `dataSequenceNumber` preservation
   (delete-file write path), concurrent-commit conflict validation (serializable isolation), and
   data-level Java interop.
2. **Schema/partition evolution + snapshot management** — `UpdateSchema`, `UpdatePartitionSpec`, **and the
   `ManageSnapshots` ref-operation surface** are now **✅** (bidirectional Java interop round-trips landed
   2026-06-07 via the `dev/java-interop/` oracle + the `crates/iceberg/tests/interop_update_schema.rs`,
   `interop_update_partition_spec.rs`, and `interop_manage_snapshots.rs` Direction-1 tests; one
   `generate`/`verify` pass covers all THREE capabilities — 7 schema + 7 partition + 7 manage-snapshots
   scenarios, both directions, all 21 PASS). The `UpdatePartitionSpec` interop surfaced (and fixed,
   in-scope) a real Rust↔Java divergence: the partition-name↔schema collision check was identity-only and
   rejected the legitimate V1 void replacement; relaxed to identity-OR-void (source-id-gated) to mirror
   Java's bind path. `UpdateSchema` column initial/write **defaults** are plumbed and
   `Schema::check_compatibility` (in `TableMetadataBuilder::add_schema`) now mirrors Java
   `Schema.checkCompatibility` in FULL — both the **V3-only initial-default guard** (rejects a non-null
   `initial_default` below v3) AND the **V3-only type gate** (rejects a `timestamp_ns`/`timestamptz_ns`
   field, incl. nested, below v3). For `ManageSnapshots`, both the **"Snapshot model + refs"** row and the
   **ref-operation surface** of the **"Snapshot management"** row are now ✅; **`cherrypick` landed 2026-06-11**
   (`transaction/cherry_pick.rs` — the WAP publish commit replaying data files via `SnapshotProducer`) and is
   **🟡 unit-proven, interop deferred** — it is a standalone action, not part of the `manage_snapshots`
   ref-op surface. The manage-snapshots interop surfaced a
   read divergence the Rust V2/V3 snapshot reader required `sequence-number`, while the spec
   (`format/spec.md` lines 1979 & 2002) and Java's `SnapshotParser` both default an absent
   `sequence-number` to 0 on read; **FIXED 2026-06-07** with `#[serde(default)]` on
   `_serde::SnapshotV2`/`SnapshotV3.sequence_number`, so Rust now reads the seq-0 V1→V2-upgrade-carryover
   table class Java emits. The sibling spec-mandated default-to-0 read fields on the **Avro manifest /
   manifest-list path** — manifest-list `content`/`sequence-number`/`min-sequence-number`; manifest-entry
   `sequence_number`/`file_sequence_number`; data-file `content` — were **VERIFIED already-correct and pinned
   2026-06-07** (per-field empirical proof + mutation-verified regression tests; see the "Manifest +
   manifest-list read/write" row). The table-metadata **`last-sequence-number`** sibling is CLOSED (Arc G
   2026-06-11): Rust's strict-required-on-V2/V3 read ALREADY MATCHES Java 1.10.0 — bytecode shows
   `TableMetadataParser.fromJson` reads it via `JsonUtil.getLong` (THROWS when absent) for `formatVersion>1`
   and hard-defaults to 0 for V1; spec line 179 explicitly permits the V2 throw. The proposed
   `#[serde(default)]` "fix" would have DIVERGED (accepting V2 metadata Java rejects); strict is correct and
   is now pinned.
3. **Format & type breadth** — ORC + Avro data files; remaining V3 types (variant, geo, unknown). The
   V3-only **type** gate (Java `Schema.MIN_FORMAT_VERSIONS`: `timestamp_ns`/`variant`/`unknown`/`geometry`/
   `geography` require v3) is now **enforced for the representable types** (`timestamp_ns`/`timestamptz_ns`,
   and `variant` since F1 2026-06-11) in `Schema::check_compatibility`; `unknown`/`geometry`/`geography`
   get a one-line `min_format_version` arm each when those types land (tracked in `task/todo.md`).
   (`timestamp_ns` and column default values already landed in the 0.8/0.9 base — see the matrix.)
4. **Views in catalogs** (`ViewCatalog` + view operations).
5. **Maintenance actions** (expire/orphan/compaction/rewrite-deletes/compute-stats/migrate).
6. **Encryption** (`EncryptionManager`, KMS, encrypted FileIO/manifests).
</content>
</invoke>
