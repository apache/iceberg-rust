<!--
  ~ Licensed to the Apache Software Foundation (ASF) under one
  ~ or more contributor license agreements.  See the NOTICE file
  ~ distributed with this work for additional information
  ~ regarding copyright ownership.  The ASF licenses this file
  ~ to you under the Apache License, Version 2.0 (the
  ~ "License"); you may not use this file except in compliance
  ~ with the License.  You may obtain a copy of the License at
  ~
  ~   http://www.apache.org/licenses/LICENSE-2.0
  ~
  ~ Unless required by applicable law or agreed to in writing,
  ~ software distributed under the License is distributed on an
  ~ "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
  ~ KIND, either express or implied.  See the License for the
  ~ specific language governing permissions and limitations
  ~ under the License.
-->

# Java ↔ Rust Capability Gap Matrix

> **Goal:** 1-to-1 capability parity between this Rust implementation and the Apache Iceberg
> **Java `iceberg-core` / `iceberg-api`** library (the engine-agnostic table-format library — *not*
> the Spark engine integration). This is a **living document**: re-run the audit after every upstream
> sync and after each parity phase lands.

## Status legend

- ✅ **present** — implemented to a usable degree
- 🟡 **partial** — exists but incomplete vs Java
- ❌ **missing** — not implemented

## Audit provenance

- **Rust base audited:** owned fork on upstream **`iceberg` 0.9.1** (datafusion 52.2, arrow 57.1,
  parquet 57.1, MSRV 1.92), **re-audited 2026-06-07** after the Phase 0 sync. Source of truth:
  `crates/iceberg/src/{spec,expr,scan,transaction,writer,arrow,io,inspect,puffin,catalog}` plus the
  new `crates/storage/opendal` FileIO crate.
- **Java reference:** `apache/iceberg` `main`, modules `api/` + `core/` + `data/` + `orc/` +
  `parquet/` + `arrow/`.
- **Re-audit policy:** re-run after every upstream sync and after each parity phase; date-stamp this
  block and strike the rows the sync/phase solved.
- **Interop oracle (test-only):** `dev/java-interop/` drives Java `iceberg-core` **1.10.0** as a
  read/write oracle for bidirectional fixtures (not shipped, not a Cargo dependency). One
  `generate`/`verify` pass now covers all THREE Phase-1 evolution capabilities — it backs the
  ✅-by-interop rows `UpdateSchema`, `UpdatePartitionSpec`, and the `ManageSnapshots` ref-operation surface
  (incl. "Snapshot model + refs"), all dated 2026-06-07.
- **What the 0.7→0.9.1 sync changed (flipped rows below):** `timestamp_ns` type, column default
  values (`initial_default`/`write_default`), merge-on-read **read** application of position-deletes +
  deletion-vectors during scan, the `upgrade_format_version` transaction action, and a real
  `TransactionAction`/`ApplyTransactionAction` extension seam. The **headline gaps are unchanged**:
  write-engine actions, schema/partition/snapshot evolution, incremental scans, ORC/Avro data files,
  variant/geo/unknown types, catalog view ops, maintenance actions, encryption.

## Matrix

| Area | Status | Java reference | Rust location / note |
|---|---|---|---|
| Primitive + nested types | ✅ | `api/.../types/Types.java` | `spec/datatypes.rs` |
| V3 types: variant | ❌ | `api/.../variants/` | none |
| V3 types: geometry / geography | ❌ | `api/.../geospatial/` | none |
| V3 types: timestamp_ns | ✅ | `types/Types.java` | `spec/datatypes.rs` (`PrimitiveType::TimestampNs`/`TimestamptzNs`); **V3-only format-version gate enforced** — `Schema::check_compatibility` rejects a `timestamp_ns`/`timestamptz_ns` field (incl. nested) on a V1/V2 table (Java `Schema.MIN_FORMAT_VERSIONS`, "Invalid type for {col}: timestamp_ns is not supported until v3") |
| V3 types: unknown | ❌ | `types/Types.java` | none |
| Column default values (initial/write) | ✅ | `Schema`/`Types` | `spec/datatypes.rs` `NestedField` carries `initial_default`/`write_default` |
| Partition transforms (identity/bucket/truncate/year/month/day/hour/void) | ✅ | `api/.../transforms/` | `spec/transform.rs` |
| Schema evolution (`UpdateSchema`) | ✅ | `api/UpdateSchema.java`, `core/SchemaUpdate.java`, `schema/UnionByNameVisitor.java` | `transaction/update_schema.rs`: add/add-required (allow-incompatible gated), rename, update-type (promotion-gated), update-doc, make-optional/require, delete, move first/before/after (struct-local, cross-struct rejected), set-identifier-fields (exists/required/primitive/not-deleted rules; id-stable across rename/move), `union_by_name_with` at full `UnionByNameVisitor` parity (add new incl. nested under list/map structs; relax required→optional; legal promotion; reject incompatible primitive + complex↔primitive type changes; doc; mirrored no-op). Fresh nested field-id assignment is **level-order** (Java `AssignFreshIds`/`CustomOrderSchemaVisitor`: struct assigns all immediate ids then descends; map assigns key-id then value-id first) — pinned by `testAddNestedMapOfStructs`/`testAddNestedListOfStructs` exact-id tests. Case-insensitive lowercase-name collisions rejected at `Schema::build` (`Cannot build lower case index: a and b collide`, Java `TypeUtil.indexByLowerCaseName`). Column **initial/write defaults** plumbed through the builder API (Java `addColumn(..,Literal)` / `addRequiredColumn(..,default)` / `updateColumnDefault(..,Literal)` overloads): an add sets BOTH `initial_default` and `write_default`, a **required add WITH a default is allowed without `allow_incompatible_changes`** (the default backfills existing rows), and `update_column_default` sets only `write_default` on an existing field; defaults are type-validated (reject non-primitive "Invalid default value..."; reject mismatched primitive "Cannot cast default value to..."). A defaulted optional add can be made required without `allow_incompatible_changes` (its initial default backfills existing rows); `update_column_default` sets only the write default, so an add+`update_column_default`+require is still rejected (Java `testAddColumnWith[UpdateColumn]DefaultToRequiredColumn`). Emits `AddSchema`+`SetCurrentSchema{-1}` with `LastAssignedFieldIdMatch`+`CurrentSchemaIdMatch`. 75 unit tests (+ 2 schema-build collision tests + 7 `Schema::check_compatibility` tests [2 initial-default + 5 V3-type gate] + 5 `add_schema` V3-default-guard tests). **Interop ✅ (2026-06-07):** bidirectional Java round-trip via the `dev/java-interop/` oracle (Java `iceberg-core` 1.10.0, package-private `@VisibleForTesting SchemaUpdate(Schema,int)` ctor) + `crates/iceberg/tests/interop_update_schema.rs` over 7 committed scenarios (`add_top_level_columns`, `add_nested_struct_and_map` [the level-order nested-id case], `rename_and_move`, `update_type_promotion`, `make_optional_and_delete`, `set_identifier_fields`, `add_required_with_default_and_update_default`). Direction 1 (Rust reproduces Java's evolution) runs offline; Direction 2 (`mvn ... verify`) asserts Java reads the Rust-written metadata — 7/7 PASS both directions. **V3 initial-default guard enforced:** column initial defaults are V3-only in Java — `Schema.checkCompatibility(schema, formatVersion)` (called on every add-schema build path via `addSchemaInternal`) rejects a non-null `initialDefault` when `formatVersion < 3` ("...non-null default (...) is not supported until v3"). The Rust side now mirrors this exactly: `Schema::check_compatibility(format_version)` (`spec/schema/mod.rs`) is wired into `TableMetadataBuilder::add_schema` — the single choke point every add-schema path flows through — and iterates ALL fields incl. nested (via the recursive id→field index, the analogue of Java's `lazyIdToField()`), rejecting `add_required_column_with_default(..)` on a V1/V2 table with `ErrorKind::DataInvalid`. Only `initial_default` is gated (not `write_default`, matching Java). The two default-bearing interop scenarios use a V3 base (matching Java's contract); the V2 rejection is pinned both at the metadata-builder layer (`table_metadata_builder.rs`: top-level + nested rejected, V3 allowed, write-default-only allowed, no-default unaffected) and end-to-end through the catalog commit path (`interop_update_schema.rs::test_v2_default_is_rejected_by_v3_guard`). The capability is interop-proven in both directions with the guard in place. **V3-only type gate enforced too:** `Schema::check_compatibility` now mirrors Java `Schema.checkCompatibility` in FULL — the same single pass that gates initial-defaults also gates V3-only **types** via `MIN_FORMAT_VERSIONS`, rejecting a `timestamp_ns`/`timestamptz_ns` field (incl. nested, dotted path in the message) on a V1/V2 table ("Invalid type for {col}: timestamp_ns is not supported until v3"); a field that violates BOTH rules accumulates BOTH problems into the single combined "Invalid schema for v{N}:" error (ordered by field id, mirroring Java's TreeMap). _(Of Java's five `MIN_FORMAT_VERSIONS` types only `timestamp_ns`/`timestamptz_ns` (Java `TIMESTAMP_NANO`) are representable in Rust today; `variant`/`unknown`/`geometry`/`geography` get a one-line arm each in `min_format_version` when those types land — tracked in `task/todo.md`.)_ |
| Partition evolution (`UpdatePartitionSpec`) | ✅ | `api/UpdatePartitionSpec.java`, `core/BaseUpdatePartitionSpec.java` | `transaction/update_partition_spec.rs`: add/add-with-transform (Java `PartitionNameGenerator` auto-naming), remove-by-name/by-transform, rename, `add_non_default_spec`, `case_sensitive`; full `BaseUpdatePartitionSpec` parity — dup-name/redundant-time-transform/remove-newly-added/rename+delete guards, delete-then-readd rewrite, V1 alwaysNull (void) replacement, and `recycleOrCreatePartitionField` (recycles a historical field's id AND name on a `(source,transform)` match). Emits `AddSpec`+`SetDefaultSpec{-1}` with `LastAssignedPartitionIdMatch` always + `DefaultSpecIdMatch` only when the spec is set as default (Java `UpdateRequirements`). Reviewed against `TestUpdatePartitionSpec.java` (28 unit tests incl. end-to-end builder round-trip + no-op dedup). **Interop ✅ (2026-06-07):** bidirectional Java round-trip via the `dev/java-interop/` oracle (Java `iceberg-core` 1.10.0) + `crates/iceberg/tests/interop_update_partition_spec.rs` over 7 committed scenarios (`add_identity_field`, `add_transform_fields` [auto-names + sequential field-ids], `remove_field_v2` [omit], `remove_field_v1_void` [V1 void replacement preserves field id], `rename_field`, `field_id_recycling` [recycles historical id 1001 **and** name `id_shard`], `delete_then_readd` [un-delete → dedup to existing spec id]). The oracle drives a REAL `BaseUpdatePartitionSpec` via `BaseTable(ops).updateSpec()…commit()` over an in-memory `TableOperations` (so `base != null` and the recycling path is live — the `@VisibleForTesting` ctors set `base = null` and skip it). Direction 1 (Rust reproduces Java's evolved default spec — spec-id + each field's source-id/field-id/name/transform + last-partition-id) runs offline; Direction 2 (`mvn ... verify`) asserts Java reads the Rust-written metadata — 7/7 PASS both directions. **Production guard fixed (in-scope, surfaced by interop):** the partition-name↔schema collision check (`PartitionSpecBuilder::check_name_does_not_collide_with_schema` + `TableMetadataBuilder::validate_partition_field_names`) was identity-only and wrongly rejected the V1 **void replacement** (`void(category)` named `category`, sourced from its own column) when its emitted spec is bound — Java's bind-path `checkAndAddPartitionName(name, sourceId)` permits a non-identity transform as long as the name↔source-id correspondence holds. Relaxed to **identity OR void** (source-id-gated), mirroring Java; non-identity/non-void name collisions still rejected. Pinned with positive + negative tests at both layers (mutation-verified load-bearing). |
| Sort order (`ReplaceSortOrder`) | ✅ | `api/ReplaceSortOrder.java` | `transaction/sort_order.rs` |
| Snapshot model + refs (branches/tags) | ✅ | `api/Snapshot.java`, `SnapshotRef.java`, `core/SnapshotParser.java` | spec types + ref ops (`transaction/manage_snapshots.rs`). **Interop ✅ (2026-06-07):** the `SnapshotReference`/`SnapshotRetention` ref model round-trips byte-for-byte with Java via the `dev/java-interop/` oracle + `crates/iceberg/tests/interop_manage_snapshots.rs` — for every one of the 7 ManageSnapshots scenarios, the evolved refs map (each ref's snapshot-id + branch-vs-tag kind + min_snapshots_to_keep / max_snapshot_age_ms / max_ref_age_ms) and the current-snapshot-id are structurally equal to Java's, both directions (Direction 1 offline; Direction 2 `mvn ... verify` reads the Rust-written metadata). 7/7 PASS both directions. **Spec-mandated lenient `sequence-number` read honored (2026-06-07):** a V2/V3 snapshot whose `sequence-number` is ABSENT now reads as 0, matching the spec's read rule (`format/spec.md` lines 1979 & 2002: "Snapshot field `sequence-number` **must default to 0** when reading v1 metadata") and Java's `SnapshotParser` (write omits the field when ≤ 0; read defaults absent → `INITIAL_SEQUENCE_NUMBER` (0)). `#[serde(default)]` on `_serde::SnapshotV2.sequence_number` and `_serde::SnapshotV3.sequence_number` (`spec/snapshot.rs`) mirrors the V1 path that already hard-codes `sequence_number: 0`. This unblocks reading the **V1→V2-upgrade carryover** table class (`upgradeFormatVersion` bumps the version without rewriting pre-upgrade snapshot seqs, so Java emits seq-0 snapshots with the field omitted — previously unreadable by Rust). Pinned by `spec/snapshot.rs` tests (V2/V3 seq-omitted → 0, seq-present → preserved; mutation-verified). Write behavior unchanged (Rust still emits the field; Java tolerates it). |
| Snapshot management (`ManageSnapshots`: branch/tag CRUD, rollback, rollback-to-time, set-current, fast-forward) | ✅ (ref-op surface; **`cherrypick` Phase-2-gated**) | `api/ManageSnapshots.java`, `core/SetSnapshotOperation.java`, `api/SnapshotRef.java` | `transaction/manage_snapshots.rs`: create/replace/remove branch+tag, rename-branch, set-current, rollback (ancestry-checked), **rollback-to-time** (Java `SetSnapshotOperation.findLatestAncestorOlderThan`: newest ancestor with `timestamp_ms` strictly `<` the arg; errors "Cannot roll back, no valid snapshot older than" if none), fast-forward, retention — with **non-positive retention rejected** (`SnapshotRef.Builder` `> 0`: "Min snapshots to keep must be greater than 0" / "Max snapshot age must be greater than 0 ms" / "Max reference age must be greater than 0") and optimistic-concurrency `RefSnapshotIdMatch` guards + unit tests. **Interop ✅ (2026-06-07):** the ref-operation surface is bidirectionally interop-proven via the `dev/java-interop/` oracle (Java `iceberg-core` 1.10.0, a REAL `SnapshotManager` driven by `BaseTable(ops).manageSnapshots()…commit()` over an in-memory `TableOperations` whose base carries a real snapshot history + refs) + `crates/iceberg/tests/interop_manage_snapshots.rs` over 7 committed scenarios — `create_branch_and_tag`, `rollback_to_ancestor`, `rollback_to_time` (strict-`<` ancestor selection → ROOT), `set_current_snapshot` (no ancestry check), `fast_forward` (branch @ROOT → CURRENT), `retention` (branch-only fields on the `dev` branch + `max_ref_age_ms` on the `stable` tag), `remove_and_rename`. Direction 1 (Rust reproduces Java's evolved refs + current-snapshot-id) runs offline; Direction 2 (`mvn ... verify`) asserts Java reads the Rust-written metadata — 7/7 PASS both directions. **`cherrypick` is NOT interop-proven and remains Phase-2-gated** — it is not a metadata-only ref op: Java `cherrypick` extends `MergingSnapshotProducer` and replays data files (gated on the write engine), so it is out of scope for this metadata-parity row and tracked under Phase 2. |
| Manifest + manifest-list read/write | ✅ | `core/.../ManifestReader/Writer` | `spec/manifest`, `spec/manifest_list.rs`. **Spec-mandated default-to-0 reads VERIFIED (2026-06-07):** all sibling fields the spec (`format/spec.md` 1980-1985) requires to default to 0 / content=data when reading a v1 manifest (list) are already honored AND now pinned — manifest-list `content`/`sequence_number`/`min_sequence_number` via `#[serde(default = ...)]` on `_serde::ManifestFileV2`/`V3` (the V2/V3 manifest-list reader uses `Reader::new`, so absent fields reach serde; mutation-verified — stripping the attribute fails "missing field `content`"); manifest-entry `sequence_number`/`file_sequence_number` via `ManifestEntryV1::try_into` (hard-codes `Some(0)`); data-file `content` via `#[serde(default)]` on `DataFileSerde.content` (the V1 manifest reads with `manifest_schema_v1`, which has no `content` column, so serde sees it absent — mutation-verified load-bearing). Pinned by `spec/manifest_list.rs` tests (V1-shaped list read as V2/V3 → all three default to 0) + `spec/manifest/writer.rs` test (genuine V1 manifest round-trip → entry seq/file-seq = `Some(0)`, data-file content = Data). NO production change was needed — the readers were already correct; the gap was test coverage of the absent-field-at-read case. |
| `RewriteManifests` | ❌ | `api/RewriteManifests.java` | none |
| Write: fast append | ✅ | `api/AppendFiles.java` | `transaction/append.rs` |
| Write: merge append | ❌ | `AppendFiles` (merge mode) | none |
| Write: `OverwriteFiles` | 🟡 | `api/OverwriteFiles.java`, `core/BaseOverwriteFiles.java`, `core/MergingSnapshotProducer.java` | `transaction/overwrite_files.rs`: `OverwriteFilesAction` (explicit add + delete in ONE `Overwrite` snapshot). `add_file`/`add_files` reach the producer exactly as fast append (validated via `validate_added_data_files` — data content type, partition-spec match, partition-value compatibility); `delete_file`/`delete_files`/`delete_data_files` resolve against the current snapshot and rewrite manifests via the **shared** `SnapshotProducer::{resolve_delete_paths, current_data_manifests}` + `process_deletes` machinery (factored out of `DeleteFiles`, Rule of Three). Both happen in one snapshot — added manifest + rewritten (filtered) manifests. Add-only and delete-only allowed; truly-empty rejected; absent / mixed-present+absent delete errors ("Missing required files to delete", Java `failMissingDeletePaths`). **Summary reflects added AND deleted file/record counts** (`SnapshotProducer::summary` now also calls `SnapshotSummaryCollector::remove_file`, mirroring Java `MergingSnapshotProducer.apply` merging the added + filter-manager summaries); a same-change fix corrects the producer's previous-snapshot resolution to the current branch head (it had looked up the not-yet-committed new snapshot id → totals always seeded from 0 → underflow on any net-removal). 11 `MemoryCatalog` unit tests assert the post-commit SCAN live set (delete B + add D → {A,C,D}), the dynamic operation, B's Deleted tombstone, surviving-entry provenance across snapshots, same-partition replace, summary counts, and cumulative running totals across snapshots. **The recorded operation is DYNAMIC, matching Java `BaseOverwriteFiles.operation()` exactly:** delete-only → `Operation::Delete`, add-only → `Operation::Append`, both → `Operation::Overwrite` (classified on the requested add/delete sets, i.e. Java `addsDataFiles()`/`deletesDataFiles()`). **Deferred to ✅:** `overwriteByRowFilter(Expression)` (needs inclusive/strict metrics evaluators), concurrent-commit conflict validation (`validateNoConflictingData`/`...Deletes`/`validateFromSnapshot` — serializable isolation), data-level Java interop round-trip. |
| Write: `ReplacePartitions` (dynamic/static overwrite) | 🟡 | `api/ReplacePartitions.java`, `core/BaseReplacePartitions.java`, `core/MergingSnapshotProducer.java`, `core/ManifestFilterManager.java` | `transaction/replace_partitions.rs`: `ReplacePartitionsAction` (DYNAMIC partition overwrite). `add_file`/`add_files` (+ `set_commit_uuid`/`set_snapshot_properties`/`set_key_metadata`). When committed, for every partition an ADDED file belongs to it replaces (deletes) all existing live data files in that same `(spec_id, partition)` tuple, then adds the new files — in ONE `Operation::Overwrite` snapshot (Java `BaseReplacePartitions.operation()` = `OVERWRITE`). Replace is **by PARTITION VALUE** (Java `ManifestFilterManager` `dropPartitions.contains(file.specId(), file.partition())`), NOT by path/row-filter — no metrics evaluators. The drop-partition set is collected from the added files; a new **by-partition** resolver `SnapshotProducer::resolve_partition_deletes` (the sibling of the by-path `resolve_delete_paths`) scans the current data manifests and returns the matching live `DataFile`s, which feed the SAME `process_deletes` rewrite/keep/drop machinery `DeleteFiles`/`OverwriteFiles` use (provenance preserved on surviving entries). Sets the `replace-partitions=true` summary property (Java `SnapshotSummary.REPLACE_PARTITIONS_PROP`). **Unpartitioned table = FULL replace** (every file is in the single empty partition → adding any file replaces all). **A replaced partition with no existing files is a pure add** (Java's `failMissingDeletePaths` guards only path deletes, never partition drops). Summary totals are Java-faithful via the existing `update_totals` (`prev + added - removed`; `truncate_full_table` stays `false` — Java has no such branch, the resolved removed-set already reports every deletion). 8 `MemoryCatalog` unit tests assert the post-commit SCAN live set: cross-partition isolation (replace x=0, x=1 untouched), multiple partitions, multiple new files per partition, surviving-entry provenance across snapshots, unpartitioned full replace (live={C}, marker set, totals 2-2+1=1), replace-empty-partition = pure add, Overwrite op + marker on partitioned replace. **Concurrent-commit conflict validation landed (2026-06-08, OPT-IN):** `validate_no_conflicting_data()` (enable) + `validate_from_snapshot(id)` (override the starting snapshot) mirror Java `BaseReplacePartitions.validate` → `MergingSnapshotProducer.validateAddedDataFiles`: when enabled, the commit is rejected (non-retryable `DataInvalid` = Java `ValidationException`) if any snapshot committed since the starting snapshot added DATA to a replaced `(spec_id, partition)`. Default (no opt-in) = snapshot isolation, behavior unchanged. 3 real concurrent-commit tests (a separate `fast_append` lands between build and commit): conflict in the replaced partition → REJECTED (asserts non-retryable + the message); concurrent append to an untouched partition → PASSES; validation OFF → concurrent append to the replaced partition → SUCCEEDS (default unchanged). **Deferred to ✅:** static `replaceByRowFilter`/explicit-partition APIs (need metrics evaluators), `validateNoConflictingDeletes` (delete-file conflict — needs the deleted-files/added-delete-files-since helpers), data-level Java interop round-trip. |
| Write: `DeleteFiles` | 🟡 | `api/DeleteFiles.java`, `core/StreamingDelete.java`, `core/ManifestFilterManager.java` | `transaction/delete_files.rs`: `DeleteFilesAction` (delete by file path / `DataFile` reference) + the foundational **manifest-filter / rewrite** machinery in `transaction/snapshot.rs` (`SnapshotProducer::process_deletes`) that `OverwriteFiles` and `ReplacePartitions` now reuse (and `RewriteFiles` will). The resolution + data-manifest-listing was factored into shared `SnapshotProducer::{resolve_delete_paths, current_data_manifests}` when `OverwriteFiles` landed (Rule of Three); `ReplacePartitions` added the by-PARTITION sibling `SnapshotProducer::resolve_partition_deletes`. Mirrors Java `ManifestFilterManager.filterManifest`: each existing data manifest containing a to-be-deleted live entry is rewritten (matching entries → `Deleted` carrying their data file + data/file seq, the rest copied forward as `Existing` preserving snapshot-id + both seq numbers — V2/V3 inheritance); a manifest with no matching entry is carried forward UNCHANGED; an all-deleted rewritten manifest is kept (Java `MergingSnapshotProducer.apply` `snapshotId()==snapshotId()` keep rule), a no-live-file manifest dropped. Delete-only commit allowed (precondition relaxed); a truly-empty commit rejected; deleting an absent path errors ("Missing required files to delete", Java `failMissingDeletePaths`). 8 `MemoryCatalog` unit tests assert the post-commit SCAN live set + manifest counts + carry-forward. **Deferred to ✅:** delete-by-row-filter / partition-predicate (Java `deleteFromRowFilter`/`dropPartition` — needs metrics evaluators, OverwriteFiles/ReplacePartitions increments); data-level Java interop round-trip (Spark/Docker = CI-only). |
| Write: `RowDelta` (merge-on-read) | 🟡 | `api/RowDelta.java`, `core/BaseRowDelta.java`, `core/MergingSnapshotProducer.java` | `transaction/row_delta.rs`: `RowDeltaAction` — the merge-on-read write commit. `add_data_files` (rows, Java `addRows`) + `add_deletes` (position/equality DELETE files, Java `addDeletes`) + `set_commit_uuid`/`set_snapshot_properties`/`set_key_metadata`, in ONE snapshot. The added delete files are written into a DELETE manifest (`ManifestContentType::Deletes`) alongside the DATA manifest, both referenced from the same manifest list — via new `SnapshotProducer` delete-manifest support (`with_added_delete_files` + `write_added_delete_manifest`, mirroring `write_added_manifest` but with the `Deletes` writer; the empty-commit precondition relaxed so an add-deletes-only commit is allowed). **Added delete entries inherit the new snapshot's sequence number at read time** (Added with no seq for V2/V3, same mechanism as added data files) so a delete added now applies to data from EARLIER snapshots (`data_seq <= delete_seq`) — the spec's merge-on-read seq rule. Delete files validated as PositionDeletes/EqualityDeletes content (a `Data` file is rejected) + partition-spec match; data files validated like fast append. **Operation is DYNAMIC, matching Java `BaseRowDelta.operation()`:** adds-data-only → `Append`, adds-deletes-only → `Delete`, both → `Overwrite`. Summary carries added data + delete file counts + position/equality-delete counts (the `SnapshotSummaryCollector::add_file` content-type branch — NO `snapshot_summary.rs` edit needed). **THE CROWN-JEWEL end-to-end test** proves the whole write→read chain: `MemoryCatalog` (real-FS `LocalFsStorageFactory`) → fast-append a REAL parquet data file (5 rows) → produce a REAL position-delete file via the 5a `PositionDeleteFileWriter` at positions {1,3} → `row_delta().add_deletes(...).commit()` → SCAN drops the deleted rows (result = {10,30,50}, not the deleted {20,40}). Mutation-verified: mangled positions, a skipped delete manifest, AND a broken seq inheritance (seq-0 delete → deleted rows resurrect) each fail the crown jewel. 9 tests total (crown jewel + delete-manifest-content + add-data+deletes-in-one + add-deletes-only + summary counts + seq inheritance + reject-Data-content + reject-spec-mismatch + reject-empty). **Deferred to ✅:** equality-delete WRITER end-to-end (the writer exists; the crown jewel focuses on POSITION deletes); `removeRows`/`removeDeletes`; concurrent-commit conflict validation (`validateFromSnapshot`/`validateNoConflictingDataFiles`/`validateNoConflictingDeleteFiles`/`validateDeletedFiles`/`validateDataFilesExist` — serializable isolation); the deletion-vector (V3 Puffin) write path (5c); data-level Java interop round-trip. |
| Write: `RewriteFiles` (compaction commit) | 🟡 | `api/RewriteFiles.java`, `core/BaseRewriteFiles.java`, `core/MergingSnapshotProducer.java`, `core/ManifestFilterManager.java` | `transaction/rewrite_files.rs`: `RewriteFilesAction` — atomically REPLACE a set of DATA files with a new set in ONE `Operation::Replace` snapshot (the compaction-commit primitive, Java `BaseRewriteFiles`). Primary entry `Transaction::rewrite_files(files_to_delete, files_to_add)` (Java `rewriteFiles(filesToDelete, filesToAdd)`) + incremental `delete_file`/`delete_files`/`add_file`/`add_files` builders (+ `set_commit_uuid`/`set_snapshot_properties`/`set_key_metadata`). The to-delete files are passed as `DataFile`s (callers hold them after a scan) and resolved **by path** against the current snapshot via the shared `SnapshotProducer::resolve_delete_paths` → the SAME `process_deletes` rewrite/keep/drop + provenance-preservation machinery `DeleteFiles`/`OverwriteFiles` use; the added files reach the producer as fast-append does. Both happen in one snapshot. **Operation is always `Operation::Replace`** (Java `BaseRewriteFiles.operation()` = `DataOperations.REPLACE`) — unlike `OverwriteFiles`' dynamic op. **Validation mirrored:** the to-delete set must be NON-EMPTY (Java `validateReplacedAndAddedFiles` "Files to delete cannot be empty") — a delete-only rewrite is legal, an add-only / fully-empty rewrite is rejected; every to-delete file must be present (Java `failMissingDeletePaths`, "Missing required files to delete"). Summary carries added + deleted counts under `Replace` (`update_snapshot_summaries` allowlist extended to admit `Operation::Replace`, mirroring Java's op-agnostic summary). 10 `MemoryCatalog` unit tests assert the post-commit SCAN live set: KEY delete[A,B]+add[D] → live {C,D} with C provenance intact + A/B Deleted tombstones; rewrite across multiple manifests; compaction-to-fewer-files (3→1); delete-absent errors; empty-delete rejected; add-without-delete rejected; delete-only allowed; summary added+deleted counts; cross-snapshot provenance preservation; incremental-builder equivalence. **Deferred to ✅:** (a) **DELETE-file (position-delete / DV) rewrite** (`deleteFile(DeleteFile)`/`addFile(DeleteFile)`) — needs the delete-file write path (later increment); DATA-file rewrite only. (b) **`dataSequenceNumber` preservation** (`setNewDataFilesDataSequenceNumber` — carry the replaced files' max data seq onto added files so outstanding merge-on-read deletes still apply) — added files get a FRESH seq (correct for a pure data rewrite with no outstanding deletes); tracked compaction-correctness follow-up paired with (a). (c) `validateFromSnapshot` / `validateNoNewDeletesForDataFiles` / concurrent-commit conflict validation. (d) data-level Java interop round-trip. |
| Transaction action extension seam | 🟡 | `core/.../BaseTransaction` | `transaction/action.rs` — `TransactionAction`/`ApplyTransactionAction` + `ActionCommit` exist (trait is `pub(crate)`; we own it → make `pub` in Phase 2) |
| Write: `upgrade_format_version` action | ✅ | format-version upgrade | `transaction/upgrade_format_version.rs` (new in 0.9) |
| Multi-op transactions + optimistic-concurrency retry | 🟡 | `api/Transaction.java`, `core/SnapshotProducer.java`, `core/MergingSnapshotProducer.java` | `catalog.update_table` retry loop (`backon`, retryable-only) + the **concurrent-commit conflict-validation FOUNDATION** (2026-06-08): `Transaction` captures `starting_snapshot_id` at `new()` (the table head) as its OWN field so it SURVIVES the staleness re-base in `do_commit`; `TransactionAction` gained a default-no-op `async fn validate(starting_snapshot_id, current)` hook run in `do_commit` AFTER the refresh/re-base and BEFORE the re-apply (so `current` is the refreshed base and an action can enumerate the concurrent commits); a validation failure is a NON-retryable `DataInvalid` (Java's non-retryable `ValidationException`) so the retry loop stops + propagates. Shared helper `added_data_files_after(table, starting_snapshot_id)` in `transaction/snapshot.rs` walks the current snapshot's parent chain back to the starting snapshot (exclusive) and collects every `Added` DATA-manifest entry from APPEND/OVERWRITE snapshots (Java `MergingSnapshotProducer.addedDataFiles`/`validationHistory`/`SnapshotUtil.ancestorsBetween` + `VALIDATE_ADDED_FILES_OPERATIONS`). First concrete check landed for `ReplacePartitions` (see that row). **Still needs:** the remaining per-action checks (sub-sequence below), validation against Glue/S3 Tables. **Conflict-validation sub-sequence:** (6) foundation + `ReplacePartitions.validateNoConflictingData` [done 🟡]; then `OverwriteFiles` `validateNoConflictingData`/`...Deletes`; `RowDelta`/`DeleteFiles` `validateDataFilesExist`; `RewriteFiles` `validateNoNewDeletes`. |
| Writer: data file | ✅ | `data/` | `writer/base_writer/data_file_writer.rs` |
| Writer: equality-delete | ✅ | `data/` | `writer/base_writer/equality_delete_writer.rs` |
| Writer: position-delete | 🟡 | `data/`, `core/.../deletes/PositionDeleteWriter.java` | `writer/base_writer/position_delete_writer.rs` — `PositionDeleteFileWriter` (Phase 2 increment 5a). Writes a position-delete file with the exact Iceberg schema (`file_path: string` id 2147483546, `pos: long` id 2147483545; reserved ids reused from `metadata_columns.rs`, no edit needed), `content(PositionDeletes)`, correct `record_count`/partition; round-trip + field-id tests prove the output is consumable by `arrow/delete_filter.rs` and Java. Java-faithful: writes records AS GIVEN (no reorder) — sorting by (file_path,pos) is the caller's job (spec recommendation doc'd). **Now end-to-end:** the `RowDelta` action (5b) commits these files and the crown-jewel test proves a scan drops the deleted rows — the full write→read merge-on-read chain works. **Stays 🟡 pending:** the optional `row` column (id 2147483544, out of scope) and a Java interop round-trip (→ ✅). |
| Writer: deletion-vector (V3 puffin DV) | 🟡 | `core/.../deletes` | `delete_vector.rs` + `puffin/` (read solid; write side partial) |
| Writer: partitioning (fanout/clustered/unpartitioned) | ✅ | — | `writer/partitioning/` |
| Read: Parquet → Arrow | ✅ | `parquet/` | `arrow/reader.rs` |
| Read: merge-on-read apply (position-deletes + DVs during scan) | ✅ | `data/.../DeleteFilter` | `arrow/delete_filter.rs`, `arrow/caching_delete_file_loader.rs`, `delete_file_index.rs` (new in 0.8/0.9) |
| Read: ORC data files | ❌ | `orc/` | none |
| Read/write: Avro data files | ❌ | `core/.../avro` (data) | Avro is manifest-only here |
| Scan planning + partition pruning | ✅ | `api/TableScan.java` | `scan/` |
| Metrics evaluators (inclusive/strict) + residual evaluation | 🟡 | `expressions/` | `expr/visitors` (partial) |
| `IncrementalAppendScan` | ❌ | `api/IncrementalAppendScan.java` | none |
| `IncrementalChangelogScan` | ❌ | `api/IncrementalChangelogScan.java` | none |
| `BatchScan` | ❌ | `api/BatchScan.java` | none |
| Scan/commit metrics reporting (`ScanReport`, `MetricsReporter`) | ❌ | `metrics/` | none |
| Catalogs: REST, Hive, Glue, S3 Tables, SQL/JDBC, in-memory | ✅ | `core/.../{rest,jdbc,inmemory}`, `aws`, `hive-metastore` | `crates/catalog/*`, `catalog/memory` |
| `ViewCatalog` + view operations (create/replace/drop/list, versions) | 🟡 | `api/catalog/ViewCatalog.java`, `api/view/` | view metadata spec + builder (`spec/view_metadata*`, `view_version.rs`) and `ViewCreation`/`ViewUpdate` types in `catalog/mod.rs`; **no `ViewCatalog` trait / no catalog view ops** (REST/Glue/etc.) |
| `SessionCatalog` | ❌ | `api/catalog/SessionCatalog.java` | none |
| `LockManager` | 🟡 | `api/LockManager.java` | partial |
| Encryption (`EncryptionManager`, KMS, encrypted FileIO/manifests) | ❌ | `api/encryption/`, `core/.../encryption` | V3 `spec/encrypted_key.rs` stub only |
| FileIO (S3/GCS/Azure/OSS/fs/memory) | ✅ | `core/.../io`, cloud modules | `io/` + extracted `crates/storage/opendal` (OpenDAL) |
| Puffin read/write + blob types (theta NDV, DV) | 🟡 | `core/.../puffin`, `api/.../puffin` | `puffin/` (blob coverage partial) |
| Maintenance: `ExpireSnapshots` | ❌ | `api/actions/ExpireSnapshots.java` | none |
| Maintenance: `DeleteOrphanFiles` | ❌ | `api/actions/DeleteOrphanFiles.java` | none |
| Maintenance: `RewriteDataFiles` (compaction) | ❌ | `api/actions/RewriteDataFiles.java` | none |
| Maintenance: `RewritePositionDeleteFiles` | ❌ | `api/actions/RewritePositionDeleteFiles.java` | none |
| Maintenance: `RemoveDanglingDeleteFiles` | ❌ | `api/actions/RemoveDanglingDeleteFiles.java` | none |
| Maintenance: `ComputeTableStats` / `ComputePartitionStats` | ❌ | `api/actions/Compute*.java` | none |
| Maintenance: `SnapshotTable` / `MigrateTable` / `RewriteTablePath` | ❌ | `api/actions/` | none |
| Partition statistics (`UpdatePartitionStatistics`, `PartitionStatisticsScan`) | ❌ | `api/Partition*Statistics*.java` | table-level stats partial |
| Table-level statistics (`UpdateStatistics`) | ✅ | `api/UpdateStatistics.java` | `transaction/update_statistics.rs` |
| Metadata inspection tables | 🟡 | `core/.../*Table` (~15 variants) | `inspect/` has snapshots + manifests + the **files family** (`files` / `data_files` / `delete_files`, 2026-06-08, Phase 3 Increment 1, `inspect/files.rs`) + the **`entries` table** (2026-06-08, Phase 3 Increment 2, `inspect/entries.rs`). The three files tables share one schema + read + row builder and differ ONLY by the manifest-content filter (Java `BaseFilesTable`: `files` = `allManifests`, `data_files` = `dataManifests`/content==DATA, `delete_files` = `deleteManifests`/content==DELETES); within a selected manifest only LIVE entries (Added/Existing, `is_alive()`) become rows. Reads the CURRENT snapshot's manifest list → manifests → `DataFile`. Arrow schema mirrors Java `DataFile.getType(partitionType).fields()` with the canonical field ids (`content`/134, `file_path`/100, `file_format`/101, `spec_id`/141, `partition`/102, `record_count`/103, `file_size_in_bytes`/104, the metrics maps `column_sizes`/`value_counts`/`null_value_counts`/`nan_value_counts` map<int,long> + `lower_bounds`/`upper_bounds` map<int,binary> with raw serialized bound bytes, `key_metadata`/131, `split_offsets`/132 list<long>, `equality_ids`/135 list<int>, `sort_order_id`/140, `first_row_id`/142, V3 DV fields `referenced_data_file`/143 + `content_offset`/144 + `content_size_in_bytes`/145). 9 unit tests (`TableTestFixture` + a DATA + a DELETE manifest): live data+delete set, content-filter exclusion both ways, content-value 0-vs-1, record_count/file_size vs committed metadata, partition struct + metrics map spot-check, Arrow schema column/type set, empty table, unpartitioned divergence. Content-filter + `is_alive()` filters mutation-verified load-bearing (REVIEWER 2026-06-08: 3 manifest-content-filter + 1 `is_alive` mutations each fail the matching test; all 21 Arrow field ids verified equal to `DataFile.getType` ids in Java order; partition value pinned PER file_path; `lower_bounds` raw LE bytes verified). **`entries`** (Java `ManifestEntriesTable` / `BaseEntriesTable`) is the raw manifest-entry diagnostic view: reads the CURRENT snapshot's `allManifests` (data AND delete) and emits EVERY entry — INCLUDING the `Deleted` tombstones (`status==2`) that `files` excludes. Schema = Java `ManifestEntry.getSchema(partitionType)` = `wrapFileSchema(DataFile.getType(...))`: 5 columns `status`(0, int, req), `snapshot_id`(1, long, opt), `sequence_number`(3, long, opt), `file_sequence_number`(4, long, opt), `data_file`(2, struct = the SAME data_file projection, NESTED). Status repr = Java `Status` `EXISTING(0)/ADDED(1)/DELETED(2)` == Rust `ManifestStatus`. The **shared data_file projection** (`data_file_fields` + `DataFileStructBuilder`) was factored into `inspect/data_file.rs` (Rule of Three, 2nd use): `files` flattens its `StructArray.columns()` to top-level columns, `entries` nests the struct under `data_file`. 6 unit tests: Deleted-tombstone PRESENT (the headline difference from `files`), status per entry (1/0/2), snapshot_id/seq/file_seq vs committed (incl. inherited Added + writer-stamped Deleted), data_file struct carries the right file per entry, Arrow schema 5 cols + field ids 0/1/3/4/2 + nested struct, empty table. `MetadataTableType::{Files,DataFiles,DeleteFiles,Entries}` + `MetadataTable::{files,data_files,delete_files,entries}` wired. **Pure-metadata tables `history` / `refs` / `metadata_log_entries`** (2026-06-08, Phase 3 Increment 3, `inspect/{history,refs,metadata_log_entries}.rs`) read only `TableMetadata` fields (no manifest IO), mirroring `inspect/snapshots.rs`. **`history`** (Java `HistoryTable`, ids verbatim): one row per snapshot-log entry — `made_current_at` ts(µs,UTC)/1 (log `timestamp_ms`×1000), `snapshot_id`/2, `parent_id`/3 (the SNAPSHOT's parent, nullable), `is_current_ancestor`/4 (true iff the snapshot id is in the parent chain walked from `current_snapshot_id` = Java `SnapshotUtil.currentAncestorIds`). **`refs`** (Java `RefsTable`): one row per branch/tag — `name`/1, `type`/2 ("BRANCH"/"TAG"), `snapshot_id`/3, `max_reference_age_in_ms`/4, `min_snapshots_to_keep`/5 (branch-only), `max_snapshot_age_in_ms`/6 (branch-only); a tag carries only `max_reference_age_in_ms`, the branch-only fields NULL; unset branch retention → NULL. **`metadata_log_entries`** (Java `MetadataLogEntriesTable`): one row per metadata-log entry (`metadata_log()` PLUS a synthetic final entry `(last_updated_ms, metadata_location)` for the CURRENT file) — `timestamp`/1 ts(µs,UTC), `file`/2, `latest_snapshot_id`/3, `latest_schema_id`/4, `latest_sequence_number`/5; the `latest_*` resolve to the snapshot current AT that timestamp (last snapshot-log entry with `made_current_at <= ts` = Java `SnapshotUtil.snapshotIdAsOfTime`), NULL when no snapshot is at/older than the timestamp (e.g. a creation-time metadata file) — FULLY implemented, nothing deferred. 10 unit tests (built from `TableMetadataV2Valid.json` + `into_builder`/`set_ref` / direct metadata-log): history Arrow schema + row-per-log-entry + snapshot-parent + `is_current_ancestor` FALSE for a forked/rolled-back snapshot (the only non-trivial column; mutation-verified) + millis→micros; refs Arrow schema + BRANCH/TAG type + retention-populated/NULL-per-kind; metadata_log_entries Arrow schema + row-per-entry+synthetic-current + `latest_*` as-of-time (NULL-before-first-snapshot / ROOT / CURRENT). `MetadataTableType::{History,Refs,MetadataLogEntries}` + `MetadataTable::{history,refs,metadata_log_entries}` + `as_str`/`TryFrom`/`all_types` wired. **The FIRST aggregating table `partitions`** (2026-06-08, Phase 3 Increment 4, `inspect/partitions.rs`, Java `PartitionsTable`): a per-partition rollup over the CURRENT snapshot's LIVE manifest entries (data AND delete, `is_alive()`), grouped by the file's partition `Struct` (which derives `Hash`+`Eq`). Schema = 11 fields in Java COLUMN order with the EXACT (non-sequential) Java field ids confirmed from `PartitionsTable.java:42-92`: `partition`/1 (struct = `default_partition_type`, REQUIRED), `spec_id`/4 int, `record_count`/2 long, `file_count`/3 int, `total_data_file_size_in_bytes`/11 long, `position_delete_record_count`/5 long, `position_delete_file_count`/6 int, `equality_delete_record_count`/7 long, `equality_delete_file_count`/8 int, `last_updated_at`/9 timestamptz OPT (`Timestamp(µs,"+00:00")`), `last_updated_snapshot_id`/10 long OPT. Aggregation mirrors `PartitionsTable.Partition.update` (`PartitionsTable.java:331-360`): DATA → record/file/size; POSITION_DELETES → pos counts; EQUALITY_DELETES → eq counts; `last_updated_*`+`spec_id` from the most-recent-snapshot-commit-time file (commit time = `snapshot.timestamp_ms()*1000` MICROS, strict `>` so the first writer wins a tie). Rows sorted deterministically by partition value; empty table → 0 rows. The partition column reuses the shared `inspect::data_file::append_partition` (promoted to `pub(super)`, Rule-of-Three 3rd use). 12 unit tests, each named for the risk it pins (same-partition summed, multi-partition sorted, pos+eq delete counted separately from data **with `total_data_file_size_in_bytes` pinned to DATA-only sizes**, Deleted-tombstone excluded, last_updated = most-recent-commit, **exact-commit-time tie keeps the first-seen file (strict `>`)**, **partition with ONLY delete files still emits a row with 0 data columns**, empty=0, unpartitioned root row, Arrow schema cols+ids+types, file spec_id, **direct `compare_partition_values` multi-field/null-first/non-long-type unit test**). Mutations caught (REVIEWER 2026-06-08, all re-verified by injection): content-type switch (count a pos-delete as data → record_count test fails); the comparison both directions — `>`→`<` makes the OLDEST snapshot win (last_updated test fails) AND `>`→`>=` lets a later equal-time file overwrite (the new tie test fails); `is_alive()` bypass (tombstone counted → exclusion test fails); delete-file size leaked into `total_data_file_size_in_bytes` (the strengthened delete test fails); swapped adjacent field ids (Arrow-schema test fails); flipped null ordering in the comparator (comparator unit test fails). **Two scoping DECISIONS (Java divergences, tested + documented):** **(1) unpartitioned partition column —** Java `PartitionsTable.schema()` DROPS the `partition` column for an unpartitioned table; the Rust port KEEPS an empty-struct (`Struct([])`) column to match the `files`-family precedent (ONE consistent module-wide unpartitioned-column divergence), pinned by `test_partitions_table_unpartitioned_keeps_empty_partition_struct_known_divergence` (flips to assert-absent when the module-wide drop-empty rule lands). **(2) multi-spec partition evolution — DEFERRED:** Java unifies all specs into one partition type via `Partitioning.partitionType(table)` + `PartitionUtil.coercePartition`; the Rust core has NO cross-spec partition-type unifier (only `default_partition_type`), so `partitions` keys rows by the file's OWN partition `Struct` and uses the default partition type for the column schema — correct for the single-spec case, a KNOWN DIVERGENCE under partition evolution (rows not coerced into a unified type). The per-file `spec_id` is still reported, so nothing is silently misattributed within a single spec; tracked until a `Partitioning.partitionType` analogue lands. **Deferred:** `readable_metrics` (Java `MetricsUtil.readableMetricsStruct` virtual per-column struct); the `all_entries`/`all_*` variants; cross-spec `partitions` unification (decision 2 above); timezone-tagged partition types (`timestamptz`/`timestamptz_ns` partition columns return `FeatureUnsupported`); **UNPARTITIONED-table partition column — KNOWN DIVERGENCE (found 2026-06-08 REVIEW, now module-wide across `files` + `partitions`):** Java `BaseFilesTable.schema()`/`PartitionsTable.schema()` DROP the `partition` field when the partition type is empty (`TypeUtil.selectNot`/`select`); the Rust port keeps an empty-struct (`Struct([])`) `partition` column. Non-corrupting (rows + every other column correct, no panic) but a schema-shape difference relevant to interop — pinned by `test_files_table_unpartitioned_keeps_empty_partition_struct_known_divergence` + `test_partitions_table_unpartitioned_keeps_empty_partition_struct_known_divergence` (both flip to assert-absent when fixed). **The cross-snapshot `all_*` file/entry tables `all_data_files` / `all_delete_files` / `all_files` / `all_entries`** (2026-06-08, Phase 3 Increment 5a, `inspect/{files,entries}.rs` + new shared `inspect/manifest_source.rs`) landed 🟡. They produce the SAME projection/rows/schema (same field ids, same partition type) as their current-snapshot counterparts (`data_files`/`delete_files`/`files`/`entries`) — the per-manifest content filter (`FilesTableKind`) and live-entry filter (`is_alive()` for files / none for entries) are byte-identical — but the manifest SOURCE becomes the **deduplicated union of manifests reachable from ALL snapshots** instead of just the current snapshot (Java `BaseAllMetadataTableScan.reachableManifests`: `Sets.newHashSet` over `table().snapshots()` of `dataManifests`/`deleteManifests`/`allManifests`/`allManifests` respectively, manifest equality by path). The scope was modeled as a new `MetadataScope {CurrentSnapshot, AllSnapshots}` enum ORTHOGONAL to `FilesTableKind`, with the manifest source factored into ONE shared `pub(super) collect_manifest_files(table, scope)` helper (`inspect/manifest_source.rs`) so `files.rs` and `entries.rs` cannot drift — `AllSnapshots` walks `metadata.snapshots()`, loads each `load_manifest_list`, and deduplicates `ManifestFile`s by `manifest_path` (a `HashSet` seen-set, FIRST-SEEN order preserved for deterministic output; Java's `HashSet` is unordered, a strict-superset choice). **Manifests are deduplicated; the FILES inside them are NOT** (Java javadoc: "may return duplicate rows"). Existing current-snapshot behavior is byte-identical (the helper's `CurrentSnapshot` arm is the old current-snapshot read). `MetadataTableType::{AllFiles,AllDataFiles,AllDeleteFiles,AllEntries}` (+ `as_str` "all_files"/"all_data_files"/"all_delete_files"/"all_entries", `TryFrom`, `all_types` via `EnumIter`) + `MetadataTable::{all_files,all_data_files,all_delete_files,all_entries}` accessors wired. 12 unit tests over a MULTI-snapshot fixture (an OLD snapshot's manifest holding a file the current snapshot does NOT, a manifest SHARED by both snapshots' lists): cross-snapshot inclusion (`all_files`/`all_data_files` include the old-only file, current `files`/`data_files` exclude it), manifest dedup (a manifest shared by 2 snapshots → its file/entry appears ONCE, NOT twice), content filters still hold across snapshots (`all_data_files` excludes delete files, `all_delete_files` excludes data files), `all_entries` shows Deleted tombstones from across snapshots, Arrow schema parity (`all_*` schema == its non-all counterpart's), empty table, AND a file reachable only from a tracked NON-ANCESTOR (forked sibling) snapshot appears in `all_files` (`test_all_files_includes_file_from_non_ancestor_snapshot`, REVIEWER 2026-06-08). Mutations caught (REVIEWER 2026-06-08, all re-run by injection): dropping the dedup seen-set (a shared manifest's file/entry doubles → both dedup tests fail), making `AllSnapshots` read only the current snapshot (the old-only file/entry vanishes → all 3 cross-snapshot-inclusion tests fail), making `AllSnapshots` walk only the current snapshot's ANCESTRY instead of all `metadata.snapshots()` (the forked-sibling file vanishes → the new non-ancestor test fails — this mutation SURVIVED the builder's original 11 tests because the base fixture's only other snapshot is the current's parent), swapping the content filter under AllSnapshots (`all_data_files` reads DELETE manifests → both the all- and current-snapshot content tests fail). The `CurrentSnapshot` helper arm is verified byte-identical (dropping a manifest from it fails the Increment-1/2 current-snapshot tests). Dedup-by-path confirmed 1:1 with Java `GenericManifestFile.equals`/`hashCode` (manifestPath only). **The LAST inspection table `all_manifests`** (2026-06-08, Phase 3 Increment 5b, `inspect/all_manifests.rs`) landed 🟡 — distinct machinery from the file/entry `all_*`: it exposes EVERY manifest referenced by ANY snapshot, **one row per (manifest × referencing snapshot), NOT deduplicated** (Java `AllManifestsTable` javadoc "may return duplicate rows"; a manifest shared by two snapshots → TWO rows). It iterates `metadata.snapshots()` directly (the 5a `manifest_source::collect_manifest_files` helper is NOT reused — that dedups AND drops the snapshot identity), loads each snapshot's manifest list, and tags each row with that snapshot's id as `reference_snapshot_id`. Schema = Java `AllManifestsTable.MANIFEST_FILE_SCHEMA` (ids + nullability, which DIFFER from the regular `manifests` table — the three `*_delete_files_count` columns are REQUIRED here): `content`/14 req, `path`/1 req, `length`/2 req, `partition_spec_id`/3 opt, `added_snapshot_id`/4 opt, `added_data_files_count`/5 opt, `existing_data_files_count`/6 opt, `deleted_data_files_count`/7 opt, `added_delete_files_count`/15 req, `existing_delete_files_count`/16 req, `deleted_delete_files_count`/17 req, `partition_summaries`/8 opt (element/9 struct: `contains_null`/10 req, `contains_nan`/11 **opt — deliberate divergence from Java's nominal `required`**, `lower_bound`/12 opt, `upper_bound`/13 opt), `reference_snapshot_id`/18 req, `key_metadata`/19 opt (Binary→Arrow LargeBinary). **`contains_nan` divergence (REVIEWER 2026-06-08):** Java declares the field `required` but `PartitionFieldSummary.containsNaN()` returns a nullable `Boolean` that is `null` when the manifest carries no NaN info (V1, or V2 without it), and Java's loosely-typed `StaticDataTask.Row` emits that null into the nominally-required cell without enforcement. Arrow ENFORCES non-nullability at array-build time, so the builder's verbatim-`required` schema PANICKED ("Found unmasked nulls for non-nullable StructArray field \"contains_nan\"") on the common `contains_nan == None` case; the reviewer made it `optional` (faithfully carrying Java's emitted null; consistent with the regular `manifests` table, whose Java schema marks the same field optional) and added `test_all_manifests_renders_null_contains_nan_without_panicking` (mutation-pinned: reverting to `required` re-triggers the panic). Counts are **CONTENT-GATED** (Java `manifestFileToRow`): a DATA manifest carries its added/existing/deleted counts in the *_data_files_count columns (delete columns 0), a DELETE manifest the reverse. **Surfaced + FIXED a real content-gating bug in the regular `manifests` table** (`inspect/manifests.rs`): its scan appended each manifest's counts to BOTH the data AND delete column families UNCONDITIONALLY, whereas Java `ManifestsTable.manifestFileToRow` (core L108-113) content-gates IDENTICALLY to `AllManifestsTable` — fix is the same `is_data` gate; its `expect_test` block updated (delete columns of the fixture's DATA manifest now 0/0/0) and mutation-pinned. The shared partition-summary builder + `FieldSummary`→(contains_null/nan + lower/upper-bound-as-STRING) conversion was factored into a new `pub(super)` `inspect/partition_summary.rs` (Rule of Three) used by BOTH tables. `MetadataTableType::AllManifests` (+ `as_str` "all_manifests", `TryFrom`, `all_types` via `EnumIter`) + `MetadataTable::all_manifests()` accessor + the datafusion provider arm (both `schema()`/`scan()` matches) wired. 7 unit tests over a multi-snapshot fixture (a SHARED DATA manifest listed by BOTH snapshots + a CURRENT-only DELETE manifest with key_metadata): per-snapshot non-dedup (shared manifest → 2 rows, different `reference_snapshot_id`), content-gated counts (DATA→data columns, DELETE→delete columns, the other family 0), `reference_snapshot_id` distinct from `added_snapshot_id` (shared manifest's `added_snapshot_id` is the PARENT while its current-snapshot row's `reference_snapshot_id` is CURRENT), key_metadata present-when-set/null-when-absent, partition-summary lower/upper bounds rendered as strings ("100"/"300"), Arrow schema 14 cols + exact ids + nullability incl. reference_snapshot_id/18 (req) + key_metadata/19 (opt, LargeBinary), empty table → 0 rows. Mutations caught (builder, all re-run by injection): un-gate the counts (write into both column families → content-gating test fails), read only the current snapshot (parent row vanishes → non-dedup test fails), dedup by `manifest_path` 5a-style (shared row collapses → non-dedup test fails), emit `added_snapshot_id` for `reference_snapshot_id` (ref-id test fails); plus un-gating the regular `manifests` scan fails its `expect_test`. **The inspection-table SET IS NOW COMPLETE** (snapshots, manifests, files family, entries, history/refs/metadata_log_entries, partitions, all_data_files/all_delete_files/all_files/all_entries + all_manifests) — remaining inspection work is `readable_metrics` + Java/Spark interop. Java/Spark interop comparison of the inspection output (🟡, interop deferred). |
| Name mapping (schema-less Parquet) | ✅ | `mapping/` | `spec/name_mapping/` |
| Events / listeners | ❌ | `api/events/`, `core/.../events` | none |
| Type utilities (prune/assign-ids/reassign/check-compat) | 🟡 | `types/TypeUtil.java` etc. | partial |

## Headline gaps (ranked by effort × value)

1. **Write engine** — everything beyond fast-append (`RowDelta`, `RewriteManifests`, merge append).
   **Started 2026-06-07: `DeleteFiles` 🟡 + `OverwriteFiles` 🟡 + `ReplacePartitions` 🟡 + `RewriteFiles` 🟡.**
   `DeleteFiles` delete-by-path/reference + the foundational manifest-filter / rewrite machinery in
   `SnapshotProducer::process_deletes`; `OverwriteFiles` composes that delete path with the fast-append
   add path in one `Overwrite` snapshot, reusing the now-shared `SnapshotProducer::{resolve_delete_paths,
   current_data_manifests}` (factored out of `DeleteFiles`); `ReplacePartitions` (dynamic partition
   overwrite) adds a by-PARTITION sibling resolver `SnapshotProducer::resolve_partition_deletes` that feeds
   the SAME `process_deletes` rewrite path (every partition an added file belongs to is replaced; an
   unpartitioned table is a full replace; `replace-partitions=true` summary marker); `RewriteFiles` (the
   compaction-commit primitive) replaces a set of DATA files with a new set in one `Replace` snapshot,
   reusing the by-path `resolve_delete_paths` + `process_deletes` machinery unchanged (Java
   `BaseRewriteFiles`: `operation()` = `REPLACE`, delete set must be non-empty, `failMissingDeletePaths`).
   All four `MemoryCatalog`-tested (post-commit SCAN live set + cross-partition isolation + provenance +
   compaction-to-fewer-files). Deferred: `overwriteByRowFilter` / delete-by-row-filter / static
   `replaceByRowFilter` (metrics evaluators), DELETE-file rewrite + `dataSequenceNumber` preservation
   (delete-file write path), concurrent-commit conflict validation (serializable isolation), and
   data-level Java interop.
2. **Schema/partition evolution + snapshot management** — `UpdateSchema`, `UpdatePartitionSpec`, **and the
   `ManageSnapshots` ref-operation surface** are now **✅** (bidirectional Java interop round-trips landed
   2026-06-07 via the `dev/java-interop/` oracle + the `crates/iceberg/tests/interop_update_schema.rs`,
   `interop_update_partition_spec.rs`, and `interop_manage_snapshots.rs` Direction-1 tests; one
   `generate`/`verify` pass covers all THREE capabilities — 7 schema + 7 partition + 7 manage-snapshots
   scenarios, both directions, all 21 PASS). The `UpdatePartitionSpec` interop surfaced (and fixed,
   in-scope) a real Rust↔Java divergence: the partition-name↔schema collision check was identity-only and
   rejected the legitimate V1 void replacement; relaxed to identity-OR-void (source-id-gated) to mirror
   Java's bind path. `UpdateSchema` column initial/write **defaults** are plumbed and
   `Schema::check_compatibility` (in `TableMetadataBuilder::add_schema`) now mirrors Java
   `Schema.checkCompatibility` in FULL — both the **V3-only initial-default guard** (rejects a non-null
   `initial_default` below v3) AND the **V3-only type gate** (rejects a `timestamp_ns`/`timestamptz_ns`
   field, incl. nested, below v3). For `ManageSnapshots`, both the **"Snapshot model + refs"** row and the
   **ref-operation surface** of the **"Snapshot management"** row are now ✅; **`cherrypick` remains
   Phase-2-gated and is NOT interop-proven** — it extends `MergingSnapshotProducer` / replays data files,
   so it is tracked under the write engine, not this metadata row. The manage-snapshots interop surfaced a
   read divergence the Rust V2/V3 snapshot reader required `sequence-number`, while the spec
   (`format/spec.md` lines 1979 & 2002) and Java's `SnapshotParser` both default an absent
   `sequence-number` to 0 on read; **FIXED 2026-06-07** with `#[serde(default)]` on
   `_serde::SnapshotV2`/`SnapshotV3.sequence_number`, so Rust now reads the seq-0 V1→V2-upgrade-carryover
   table class Java emits. The sibling spec-mandated default-to-0 read fields on the **Avro manifest /
   manifest-list path** — manifest-list `content`/`sequence-number`/`min-sequence-number`; manifest-entry
   `sequence_number`/`file_sequence_number`; data-file `content` — were **VERIFIED already-correct and pinned
   2026-06-07** (per-field empirical proof + mutation-verified regression tests; see the "Manifest +
   manifest-list read/write" row). The only residual sibling is table-metadata **`last-sequence-number`**,
   which Java ALWAYS writes for V2+ (`TableMetadataParser.toJson`), so Rust's required field never bites a
   Java-written table — a non-Java/hand-written robustness gap only, tracked in `task/todo.md`.
3. **Format & type breadth** — ORC + Avro data files; remaining V3 types (variant, geo, unknown). The
   V3-only **type** gate (Java `Schema.MIN_FORMAT_VERSIONS`: `timestamp_ns`/`variant`/`unknown`/`geometry`/
   `geography` require v3) is now **enforced for the representable types** (`timestamp_ns`/`timestamptz_ns`)
   in `Schema::check_compatibility`; `variant`/`unknown`/`geometry`/`geography` get a one-line
   `min_format_version` arm each when those types land (tracked in `task/todo.md`). (`timestamp_ns` and
   column default values already landed in the 0.8/0.9 base — see the matrix.)
4. **Views in catalogs** (`ViewCatalog` + view operations).
5. **Maintenance actions** (expire/orphan/compaction/rewrite-deletes/compute-stats/migrate).
6. **Encryption** (`EncryptionManager`, KMS, encrypted FileIO/manifests).
</content>
</invoke>
