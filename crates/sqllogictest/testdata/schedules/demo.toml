[engines]
spark = { type = "spark", url = "sc://localhost:7077" }
df = { type = "datafusion", url = "http://localhost:8181" }

[[steps]]
engine = "spark"
filename = "prepare.slt"

[[steps]]
engine = "df"
filename = "verify.sql"

