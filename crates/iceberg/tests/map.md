<!--
  ~ Licensed to the Apache Software Foundation (ASF) under one
  ~ or more contributor license agreements.  See the NOTICE file
  ~ distributed with this work for additional information
  ~ regarding copyright ownership.  The ASF licenses this file
  ~ to you under the Apache License, Version 2.0 (the
  ~ "License"); you may not use this file except in compliance
  ~ with the License.  You may obtain a copy of the License at
  ~
  ~   http://www.apache.org/licenses/LICENSE-2.0
  ~
  ~ Unless required by applicable law or agreed to in writing,
  ~ software distributed under the License is distributed on an
  ~ "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
  ~ KIND, either express or implied.  See the License for the
  ~ specific language governing permissions and limitations
  ~ under the License.
-->

# map.md — crates/iceberg/tests/

## Purpose

Integration tests of the `iceberg` crate — currently the **Rust side of the Java interop
round-trips**. Each file loads committed fixtures from `crates/iceberg/testdata/interop/`
(Java-generated by [dev/java-interop/](../../../dev/java-interop/map.md)), replays the same
operations through the public transaction/scan API against a real `MemoryCatalog` commit, and
asserts structural equality with Java's result. All run **offline** in plain `cargo test`; under
`ICEBERG_INTEROP_GEN=1` they additionally write `rust_evolved` outputs for the Java `verify` pass
(Direction 2).

## Contents

| File | Proves | Status |
|---|---|---|
| `interop_update_schema.rs` | `UpdateSchema` — 7 scenarios incl. level-order nested ids, defaults, V3 guards | ✅ both directions |
| `interop_update_partition_spec.rs` | `UpdatePartitionSpec` — 7 scenarios incl. V1 void replacement, id recycling | ✅ both directions |
| `interop_manage_snapshots.rs` | `ManageSnapshots` ref operations — 7 scenarios | ✅ both directions |
| `interop_scan_exec.rs` | data-level scan execution: Java-written parquet + position/equality deletes + partitioned MoR read by Rust, and the reverse | landed 2026-06-09 |
| `interop_dv_scan.rs` | D1 deletion-vector scan execution: Rust scans a Java-1.10.0-written **V3** table whose merge-on-read deletes are a real Puffin `deletion-vector-v1` blob (`BaseDVFileWriter`); sibling raw-blob decode pin (>2^32 positions + run containers) lives in `delete_vector::tests` (env-gated lib test, same `ICEBERG_INTEROP_DV_DIR`) | landed 2026-06-10 (Direction 1; Direction 2 with the DV writer) |
| `interop_dv_write.rs` | D2 deletion-vector WRITING (Direction 2, blob-level): Rust `DVFileWriter` writes a real Puffin DV file (3 referenced files; position 0, a 5000-run, >2^32, a dense-gap key, the array/run size-tie set {0,1,2}) for Java's production reader to verify, then asserts the Rust blobs are BYTE-IDENTICAL to Java's own serialization (env `ICEBERG_INTEROP_DV_WRITE_DIR`; phases driven by `run-interop-dv.sh` steps 4-6) | landed 2026-06-10 |
| `interop_dv_table.rs` | D4 deletion-vector TABLE level (Direction 2, the headline) + METADATA level: the GEN test COMMITS a complete V3 table (2 identity(category) partitions, 2 real parquet data files fast_appended, ONE puffin with TWO DVs via `DVFileWriter` + `row_delta`) for Java's PRODUCTION scan (`IcebergGenerics`) + manifest-API DeleteFile cross-check; the meta test asserts Rust's canonical snapshot-metadata views of BOTH sides' DV chains equal Java's own (`added-dvs`, operation, manifest/seq semantics — the E1-family 3-way) (env `ICEBERG_INTEROP_DV_TABLE_DIR`; phases driven by `run-interop-dv.sh` steps 7-11) | landed 2026-06-10 |
| `interop_dv_replace.rs` | Arc-E Inc 2 deletion-vector REPLACEMENT (Direction 2, table + metadata + blob): the GEN test commits the writer-merged replacement chain (fast_append + DV1{1} + `DVFileWriter::with_previous_deletes` merged DV2{1,3} + `remove_deletes`) for Java's PRODUCTION scan (`DvReplaceOracle`: merged DV applied, DV1 ABSENT from manifests); the meta test asserts the canonical views match all 3 ways (the FIRST LIVE `removed-dvs`); the byte test asserts the merged blob is byte-identical to Java's same merge (the Run-store tie pin) (env `ICEBERG_INTEROP_DV_REPLACE_DIR`; phases driven by `run-interop-dv.sh` steps 12-16) | landed 2026-06-10 |
| `interop_rowdelta_meta.rs` | E1 metadata-level row-delta interop: the canonical snapshot-metadata view over the 3 scan-exec fixtures (env-gated) | landed 2026-06-10 |
| `interop_write_actions_meta.rs` | E2 + Increment-4 metadata-level write-actions interop: ONE eight-step chain (delete/overwrite/replace-partitions/rewrite/rewrite-manifests/merge-append) + a delete-bearing seq-preserving `rewrite_files` fixture B, all via the canonical view (env-gated) | landed 2026-06-10 |
| `interop_cherrypick.rs` | S2 cherrypick metadata-level interop: three fixtures (ff / replay / dedup) in both directions; `snapshot_meta_view` canonical views byte-equal on both sides | landed 2026-06-11 |
| `interop_staged_wap.rs` | Z1 staged-WAP interop: three fixtures (S-ff / S-replay / S-dedup) × 2 views (staged-but-unpublished + post-publish) in both directions, BOTH sides using REAL `stage_only()` / `stageOnly()` staging APIs. GEN test (`ICEBERG_INTEROP_STAGED_WAP_GEN_DIR`) lands `rust_staged_table` + `rust_final_table`; compare test (`ICEBERG_INTEROP_STAGED_WAP_DIR`) asserts Rust's `snapshot_meta_view` of Java-produced staged + final tables equals `java_staged_meta.json` / `java_final_meta.json`. Env-gated no-ops for offline `cargo test`. | landed 2026-06-12 |
| `interop_multi_spec.rs` | Z2 multi-spec interop: SINGLE FIXTURE, BOTH DIRECTIONS. Chain: ms1 fast_append F1(spec0) → ms2 spec-evolve add identity(b) → ms3 fast_append F2(spec1) → ms4 ONE multi-spec fast_append (F0 spec0 + F3 spec1). Proves: per-spec manifest grouping, the single multi-spec commit is constructible cross-language, partition tuples rendered under each manifest's OWN spec (file's-own-spec rule), and the W3 spec-id tiebreaker at sort position 10 is the ONLY disambiguator for the two tie-shaped ms4 manifests (F0.record_count == F3.record_count == 10). GEN test (`ICEBERG_INTEROP_MULTI_SPEC_GEN_DIR`) writes the chain to `rust_table/`; compare tests (`ICEBERG_INTEROP_MULTI_SPEC_DIR`) assert Rust's canonical view of BOTH chains equals `java_meta.json`. Env-gated no-ops for offline `cargo test`. | landed 2026-06-12 |
| `interop_partition_stats.rs` | Z3 partition-stats file interop: SINGLE FIXTURE, 3 TESTS, BOTH DIRECTIONS + cross-version projection. GEN test (`ICEBERG_INTEROP_PARTITION_STATS_GEN_DIR`): builds V2 `identity(category)` table, S1 fast-append (cat=a 3rec + cat=b 2rec), S2 row-delta pos-delete (cat=a 1rec), calls `compute_and_write_stats_file` + `register_partition_stats_file`, writes `rust_table/metadata/final.metadata.json` + `expected_stats.json` (decoded rows with actual snapshot IDs). D2 test (`ICEBERG_INTEROP_PARTITION_STATS_DIR`): reads Java-written stats parquet via `read_partition_stats_file`, compares against `java_stats.json`. Cross-version test: reads Java V2 stats parquet (12 cols, no `dv_count`) against V3 schema — `project_struct_type_to_batch` null-fills `dv_count` to 0. Java D1 judgment via `verify-interop-partition-stats` in `run-interop-partition-stats.sh`. Sabotage: 4 corruptions (truncate Rust parquet; SOURCE byte-edit counter + re-read; truncate Java parquet; remove partition-statistics from metadata). Env-gated no-ops for offline `cargo test`. | landed 2026-06-12 |
| `interop_inspection.rs` / `interop_inspection_manifests.rs` | inspection tables vs Java expectations | in progress (🟡) |

## I want to...

| I want to... | go to |
|---|---|
| Add an interop test for a new capability | copy the structure of `interop_update_schema.rs` (scenario table + `apply_scenario_ops` + structural compare); scenarios must exist identically in `InteropOracle.java` |
| Regenerate the fixtures these read | [dev/java-interop/map.md](../../../dev/java-interop/map.md) |
| Understand what "equal" means here | structural equality via the spec types' `PartialEq` (schema/spec/refs), never raw bytes |

## Pointers

- **Up:** [crates/iceberg/](..) · **Related:** [dev/java-interop/map.md](../../../dev/java-interop/map.md)
  (fixture generation + Direction-2 verify), [docs/testing.md](../../../docs/testing.md) (the
  testing contract these satisfy), `testdata/interop/` (the committed fixtures).

## Debug

### Known failure modes

| Symptom | Likely cause |
|---|---|
| Interop test fails but unit tests pass | A real parity divergence — the harness's purpose; compare against the Java source, not the Rust unit-test expectations |
| Test passes locally, Direction 2 fails | Rust emits metadata Java rejects — run the `-d2` script and read Java's parse/validation error |
| Fixture file not found | Fixtures are committed under `crates/iceberg/testdata/interop/` — a new scenario needs a `generate` run first |
| Scenario drift | Rust `apply_scenario_ops` and Java `InteropOracle.scenarios()` must stay op-for-op identical — they are mirrored by hand |
| `register_table` rejects a pre-written metadata file | The catalog reads via its OWN FileIO: build `MemoryCatalog` `with_storage_factory(Arc::new(LocalFsStorageFactory))` over a tempdir, put the file under a `metadata/` subdirectory, and name it `<version>-<uuid>.metadata.json` — a bare `base.metadata.json` fails "Invalid metadata file name format" |
| Can't name an action type / call `.commit()` from an integration test | `TransactionAction` is `pub(crate)` — drive evolution through the PUBLIC path (catalog register + `Transaction` + `commit`); do NOT widen production visibility for a test. The catalog path is stronger anyway (it runs the `TableRequirement` checks) |

### First checks

- Run the single failing scenario with `-- --exact` and diff the two metadata JSONs by hand —
  field-id mismatches jump out.

### Escalate to

- [dev/java-interop/map.md#debug](../../../dev/java-interop/map.md#debug) for oracle-side issues;
  the owning source map for Rust-side divergences.
